package authbootstrap

const (
	// CollectionUsers is the MonMS operator auth collection for non-superuser accounts.
	CollectionUsers = "users"
	// CollectionAPIKeys stores per-user API keys for MCP and machine access.
	CollectionAPIKeys = "monms_api_keys"
)
