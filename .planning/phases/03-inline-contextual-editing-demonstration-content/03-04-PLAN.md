---
phase: "03"
plan: "04"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
  - "03-03"
files_modified:
  - internal/router/inline_edit_test.go
  - internal/router/handlers_test.go
  - internal/testutil/auth.go
  - workspace/EDITING-GUIDE.md
autonomous: true
requirements:
  - ICE-01
  - ICE-02
  - ICE-03
  - ICE-04
  - ICE-05
  - ICE-06
  - SEC-02
  - SEC-04
  - DEMO-01
  - DEMO-02

must_haves:
  truths:
    - "Unauthenticated GET / body excludes contenteditable and Live Editor Active"
    - "Authenticated GET / includes contenteditable, badge, hx-put, and Bearer script"
    - "Unauthenticated PUT hero_content/homepage returns status >= 400"
    - "GET / includes seeded hero title Welcome to MonMS for guests"
    - "EDITING-GUIDE.md documents login at /_/, edit flow, logout verification, re-init merge note"
  artifacts:
    - path: "internal/router/inline_edit_test.go"
      provides: "Integration tests ICE-01 through ICE-06, SEC-02, SEC-04 per D-71"
      contains: "TestInlineEdit"
    - path: "workspace/EDITING-GUIDE.md"
      provides: "Manual walkthrough per D-70"
      contains: "Live Editor Active"
    - path: "internal/testutil/auth.go"
      provides: "Superuser + Bearer client helper per D-72"
      exports: ["AuthClient"]
  key_links:
    - from: "internal/router/inline_edit_test.go"
      to: "startTestServer"
      via: "same package router; schema fixture before bootstrap"
      pattern: "startTestServer"
    - from: "internal/router/inline_edit_test.go"
      to: "/api/collections/hero_content/records/homepage"
      via: "guest PUT expects >= 400"
      pattern: "hero_content/records/homepage"
---

<objective>
Prove Phase 3 requirements with integration tests extending the Phase 2 harness and ship EDITING-GUIDE.md for operator manual verification. Closes ICE, SEC-02, SEC-04, and DEMO acceptance criteria.

Purpose: D-71, D-72, D-70 require automated evidence before phase verification.
Output: inline_edit_test.go, optional testutil auth helper, EDITING-GUIDE.md; extend startTestServer to register auth hooks like production main.go.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-inline-contextual-editing-demonstration-content/03-CONTEXT.md
@.planning/phases/03-inline-contextual-editing-demonstration-content/03-RESEARCH.md
@.planning/phases/03-inline-contextual-editing-demonstration-content/03-UI-SPEC.md
@internal/router/handlers_test.go
@internal/router/press_releases_test.go

<interfaces>
From internal/router/handlers_test.go:
```go
func startTestServer(t *testing.T, wsAbs string, opts testServerOpts) (*httptest.Server, *templates.TemplateCache, func())
// Must call router.RegisterAuthHooks(app) like main.go after 03-02
// Must copy index/base templates from workspace or use testutil scaffold
```

Auth test client per RESEARCH Code Examples:
```go
// Create _superusers record, NewAuthToken(), RoundTripper sets Authorization Bearer
```

Test names per RESEARCH Validation Architecture:
- TestInlineEdit_UnauthenticatedHidesEdit
- TestInlineEdit_AuthenticatedShowsBadge
- TestInlineEdit_AuthenticatedShowsContenteditable
- TestHeroContent_GuestPutForbidden
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test harness auth helper and startTestServer parity</name>
  <files>internal/testutil/auth.go, internal/router/handlers_test.go</files>
  <behavior>
    - AuthClient returns http.Client with Bearer transport for superuser
    - startTestServer registers RegisterAuthHooks like main.go
    - Test workspace includes hero_content.json schema before bootstrap
  </behavior>
  <action>
    Add internal/testutil/auth.go with NewSuperuser and AuthClient(t, app) per RESEARCH authenticated integration test client pattern (D-72). Update startTestServer in handlers_test.go to call router.RegisterAuthHooks(app) after pocketbase.NewWithConfig — required for cookie bridge tests in later tasks. Do not break existing handler tests.
  </action>
  <verify>
    <automated>go test ./internal/router/ -run TestServeStarts -count=1 -timeout 60s</automated>
  </verify>
  <done>Test harness matches production auth wiring per D-72.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Inline edit integration tests</name>
  <files>internal/router/inline_edit_test.go</files>
  <behavior>
    - Guest GET /: no contenteditable, no Live Editor Active, contains Welcome to MonMS (DEMO-01/DEMO-02)
    - Authed GET /: contains contenteditable, Live Editor Active, hx-put hero_content/homepage, Authorization Bearer in script, no document.cookie (SEC-04)
    - Guest PUT homepage JSON returns StatusCode >= 400 (SEC-02)
  </behavior>
  <action>
    Create internal/router/inline_edit_test.go in package router. Each test: testutil.NewWorkspace, write hero_content.json (full rules from 03-01), copy or symlink templates from workspace fixtures (index + base with Phase 3 markup — write minimal valid templates in test if workspace copies unavailable). startTestServer with isDev true. Implement TestInlineEdit_UnauthenticatedHidesEdit, TestInlineEdit_AuthenticatedShowsBadge, TestInlineEdit_AuthenticatedShowsContenteditable (can merge badge+contenteditable checks), TestHeroContent_GuestPutForbidden per D-71. Use testutil.AuthClient for authenticated GET with Authorization header. Assert SEC-04 with NotContains body document.cookie and NotContains pb_auth cookie parse patterns.
  </action>
  <verify>
    <automated>go test ./internal/router/ -run 'TestInlineEdit|TestHeroContent' -count=1 -timeout 120s</automated>
  </verify>
  <done>ICE-01 through ICE-06, SEC-02, SEC-04 automated per D-71.</done>
</task>

<task type="auto">
  <name>Task 3: EDITING-GUIDE.md operator walkthrough</name>
  <files>workspace/EDITING-GUIDE.md</files>
  <action>
    Create workspace/EDITING-GUIDE.md per D-70 and 03-UI-SPEC copywriting. Sections: (1) Login at /_/ PocketBase admin (D-53), (2) Navigate to / and confirm Live Editor Active badge, (3) Edit hero title/body inline, blur to save, (4) Logout and verify no contenteditable, (5) Troubleshooting 401/403 re-login, (6) Existing workspace manual merge note for monms init skip-if-exists (D-69). Use exact UI-SPEC strings for badge and seed copy. Not embedded in binary — workspace doc only.
  </action>
  <verify>
    <automated>test -f workspace/EDITING-GUIDE.md && grep -v '^#' workspace/EDITING-GUIDE.md | grep -c 'Live Editor Active' | grep -qx 1</automated>
  </verify>
  <done>D-70 manual test walkthrough documented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Anonymous → hero_content API | Must reject mutating requests |
| Authenticated editor → public page | Edit chrome visible only with valid auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | guest PUT | mitigate | TestHeroContent_GuestPutForbidden asserts >= 400 |
| T-03-11 | Info disclosure | edit attrs to guests | mitigate | TestInlineEdit_UnauthenticatedHidesEdit |
| T-03-12 | Spoofing | missing Bearer on HTMX | accept | Covered by template + authed GET substring test |
</threat_model>

<verification>
- go test ./internal/router/ -run 'TestInlineEdit|TestHeroContent' -count=1
- go test ./... -count=1 (phase gate)
</verification>

<success_criteria>
- All Phase 3 requirement IDs have automated or documented evidence
- Full test suite green
- EDITING-GUIDE.md complete for manual UAT
</success_criteria>

<output>
After completion, create `.planning/phases/03-inline-contextual-editing-demonstration-content/03-04-SUMMARY.md`
</output>
