---
phase: "03"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/router/auth.go
  - internal/router/ssr.go
  - internal/router/auth_test.go
  - main.go
autonomous: true
requirements:
  - ICE-02
  - ICE-05
  - SEC-04
  - DEMO-02

must_haves:
  truths:
    - "Browser GET / after admin login sets IsLoggedIn true via HttpOnly cookie bridge"
    - "AuthToken appears in SSR data only when logged in, from NewAuthToken not cookie JS"
    - "Index route passes Hero map with Title, Body, ID homepage from PocketBase record"
    - "Missing homepage record renders UI-SPEC fallback copy with slog warning, no 500"
  artifacts:
    - path: "internal/router/auth.go"
      provides: "RegisterAuthHooks — cookie set on auth, loadAuthFromCookie middleware"
      exports: ["RegisterAuthHooks"]
    - path: "internal/router/ssr.go"
      provides: "enrichSSRData with AuthToken and index-only Hero load"
      contains: "NewAuthToken"
  key_links:
    - from: "main.go"
      to: "internal/router/auth.go"
      via: "RegisterAuthHooks(app) before RegisterRoutes"
      pattern: "RegisterAuthHooks"
    - from: "internal/router/ssr.go"
      to: "app.FindRecordById"
      via: "loadHero on index slug only"
      pattern: "hero_content.*homepage"
    - from: "internal/router/auth.go"
      to: "e.Auth"
      via: "FindAuthRecordByToken from monms_auth HttpOnly cookie"
      pattern: "FindAuthRecordByToken"
---

<objective>
Implement PocketBase auth bridge for browser SSR sessions and enrich index SSR data with AuthToken and Hero fields. Without the cookie bridge, ICE-02 fails after admin login at /_/.

Purpose: D-50–D-52, D-59–D-60, and RESEARCH Pattern 6 are prerequisites for badge, inline edit, and HTMX Bearer injection.
Output: auth.go hooks/middleware, enriched ssr.go, main.go wiring, unit tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-inline-contextual-editing-demonstration-content/03-CONTEXT.md
@.planning/phases/03-inline-contextual-editing-demonstration-content/03-RESEARCH.md
@internal/router/ssr.go

<interfaces>
From internal/router/ssr.go — extend, preserve ssrData keys:
```go
func ssrData(e *core.RequestEvent, slug string) map[string]any
// IsLoggedIn: e.Auth != nil (D-50)
// Returns: IsLoggedIn, User, Slug, Path

func SSRHandler(wsAbs string, cache *templates.TemplateCache, isDev bool) func(*core.RequestEvent) error
// Replace data := ssrData(...) with enrichSSRData(e, slug, e.App)
```

From PocketBase v0.38.1 core/events.go:
```go
type RecordAuthRequestEvent struct {
    *RequestEvent
    Record *Record
    Token  string  // JWT to store in HttpOnly cookie
}
```

Token accessor per RESEARCH (not e.Auth.Token()):
```go
token, err := e.Auth.NewAuthToken()
```

Hero map shape per D-59:
```go
map[string]any{"Title": "...", "Body": "...", "ID": "homepage"}
```

Fallback copy per 03-UI-SPEC when record missing (D-60):
- Title: "MonMS is running"
- Body: "Your workspace is live. Templates load from disk — no build step required."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HttpOnly auth cookie bridge</name>
  <files>internal/router/auth.go, main.go, internal/router/auth_test.go</files>
  <behavior>
    - OnRecordAuthRequest sets HttpOnly monms_auth cookie with e.Token on successful auth
    - loadAuthFromCookie populates e.Auth when cookie valid and e.Auth nil
    - Cookie uses SameSite=Lax, Secure when TLS, Path /
    - No document.cookie parsing anywhere (SEC-04)
  </behavior>
  <action>
    Create internal/router/auth.go with RegisterAuthHooks(app core.App): (1) app.OnRecordAuthRequest().BindFunc sets monms_auth HttpOnly cookie per RESEARCH Pattern 6 — use cookie name monms_auth (RESEARCH recommendation, not pb_auth localStorage key); (2) export loadAuthFromCookie middleware that reads monms_auth, calls app.FindAuthRecordByToken(token, core.TokenTypeAuth), sets e.Auth on success. Wire RegisterAuthHooks in main.go before OnServe RegisterRoutes. Bind loadAuthFromCookie on MonMS SSR route group inside RegisterRoutes BEFORE SSRHandler runs (wrap or prepend middleware on GET /{slug...} and fragments if auth needed there — at minimum SSR catch-all). Write auth_test.go unit test: mock RequestEvent with cookie + valid token populates e.Auth.
  </action>
  <verify>
    <automated>go test ./internal/router/ -run TestAuth -count=1 -timeout 60s</automated>
  </verify>
  <done>ICE-02 browser session bridge implemented; SEC-04 HttpOnly cookie never exposed to JS.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SSR enrichSSRData with AuthToken and Hero</name>
  <files>internal/router/ssr.go, internal/router/auth_test.go</files>
  <behavior>
    - enrichSSRData adds AuthToken only when e.Auth != nil via NewAuthToken (D-51)
    - enrichSSRData adds Hero map only for slug empty or index (D-59 discretion)
    - loadHero reads hero_content/homepage; on error logs slog.Warn and returns fallback map (D-60)
    - Non-index routes omit Hero key
  </behavior>
  <action>
    Add enrichSSRData(e *core.RequestEvent, slug string) map[string]any wrapping ssrData. When e.Auth != nil, call e.Auth.NewAuthToken() per D-51 (NOT Token()). For slug == "" or slug == "index", set Hero from loadHero(e.App). loadHero uses FindRecordById hero_content homepage; map GetString title/body. SSRHandler calls enrichSSRData instead of ssrData. Extend auth_test.go or add ssr_enrich_test.go with table tests for Hero present/absent and AuthToken gating.
  </action>
  <verify>
    <automated>go test ./internal/router/ -run 'TestAuth|TestEnrichSSR' -count=1 -timeout 60s</automated>
  </verify>
  <done>DEMO-02 data layer complete; templates can bind .Hero and .AuthToken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → SSR | HttpOnly monms_auth cookie crosses boundary; JS cannot read |
| SSR HTML → Browser | AuthToken embedded in inline script for HTMX only when logged in |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Info disclosure | AuthToken in HTML | mitigate | Render only when IsLoggedIn; html/template context; JWT charset safe |
| T-03-05 | Spoofing | Cookie auth | mitigate | FindAuthRecordByToken validates JWT signature/expiry |
| T-03-06 | Tampering | CSRF on cookie session | accept | SameSite=Lax; HTMX PUT same-origin with Bearer from page |
</threat_model>

<verification>
- go test ./internal/router/ -run 'TestAuth|TestEnrichSSR' -count=1
- Manual: login at /_/ then GET / shows IsLoggedIn-driven data (verified in 03-04)
</verification>

<success_criteria>
- enrichSSRData supplies AuthToken and Hero on index when auth + record exist
- Cookie bridge sets e.Auth on document navigation without Authorization header
- Fallback hero copy on missing record without panic
</success_criteria>

<output>
After completion, create `.planning/phases/03-inline-contextual-editing-demonstration-content/03-02-SUMMARY.md`
</output>
