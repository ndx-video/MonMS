---
phase: "03"
plan: "03"
type: execute
wave: 2
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - internal/scaffold/embed/base.gohtml
  - internal/scaffold/embed/index.gohtml
  - internal/scaffold/embed/main.css
  - workspace/templates/layouts/base.gohtml
  - workspace/templates/index.gohtml
  - workspace/assets/main.css
autonomous: true
requirements:
  - ICE-01
  - ICE-03
  - ICE-04
  - ICE-06
  - DEMO-02
  - DEMO-03

must_haves:
  truths:
    - "Logged-in users see Live Editor Active badge linking to /_/"
    - "Logged-out HTML contains no contenteditable, hx-put, or editor-badge nodes"
    - "Logged-in index hero has blur-triggered HTMX PUT to hero_content/homepage"
    - "HTMX requests get Bearer header from server-rendered AuthToken script, not cookie JS"
    - "json-enc extension loaded for title field PUT"
  artifacts:
    - path: "internal/scaffold/embed/base.gohtml"
      provides: "Conditional editor badge + auth script per D-54, D-52, D-69"
      contains: "Live Editor Active"
    - path: "internal/scaffold/embed/index.gohtml"
      provides: "Hero binding + conditional inline edit attrs per D-63–D-68"
      contains: "hero_content/records/homepage"
    - path: "internal/scaffold/embed/main.css"
      provides: "Pulse animation and editable focus styles per 03-UI-SPEC"
      contains: "hero__title--editable"
  key_links:
    - from: "internal/scaffold/embed/index.gohtml"
      to: "/api/collections/hero_content/records/homepage"
      via: "hx-put on blur"
      pattern: "hx-put.*hero_content"
    - from: "internal/scaffold/embed/base.gohtml"
      to: "{{.AuthToken}}"
      via: "htmx:configRequest Authorization Bearer"
      pattern: "htmx:configRequest"
---

<objective>
Activate Phase 1 deferred inline-editing UI: Live Editor badge, server-injected HTMX Bearer script, hero contenteditable blur-save markup, and CSS affordances. Update both scaffold embed and workspace copies for immediate dev testing.

Purpose: ICE-01, ICE-03, ICE-04, ICE-06, DEMO-02, DEMO-03 require template + CSS changes per 03-UI-SPEC.md and D-54–D-69.
Output: Updated base.gohtml, index.gohtml, main.css in embed + workspace mirrors.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-inline-contextual-editing-demonstration-content/03-UI-SPEC.md
@.planning/phases/03-inline-contextual-editing-demonstration-content/03-CONTEXT.md
@internal/scaffold/embed/base.gohtml
@internal/scaffold/embed/index.gohtml
@specs/monms-prd.md

<interfaces>
Template context keys (03-UI-SPEC Template Context):
- IsLoggedIn bool — all SSR pages
- AuthToken string — logged in only (D-51)
- Hero map Title, Body, ID — index only (D-59)

CDN order (03-UI-SPEC): head unchanged; body end: (8) auth script if logged in, (9) Alpine defer.
Add json-enc script inside logged-in block BEFORE auth script (RESEARCH Pitfall 3).

PRD §5 markup adapted per D-52: NO document.cookie; use {{.AuthToken}} in script.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: base.gohtml badge and HTMX auth script</name>
  <files>internal/scaffold/embed/base.gohtml, workspace/templates/layouts/base.gohtml</files>
  <action>
    Remove Phase 1 hidden #editor-overlay placeholder entirely (D-55). Add {{if .IsLoggedIn}} block before main with .editor-badge markup exactly per 03-UI-SPEC Visuals section (role=status, aria-live=polite, dot, Live Editor Active, Full Admin Dashboard link to /_/). After main, before Alpine defer: {{if .IsLoggedIn}} include json-enc script from unpkg htmx.org@1.9.12/dist/ext/json-enc.js then inline script setting htmx:configRequest Authorization Bearer {{.AuthToken}} per D-52 — never reference document.cookie or pb_auth. Keep HTMX 1.9.12 and Alpine 3.14.8 versions unchanged (D-67). Mirror identical changes to workspace/templates/layouts/base.gohtml for dev workspace.
  </action>
  <verify>
    <automated>go run . validate --files workspace/templates/layouts/base.gohtml 2>/dev/null || monms validate --files workspace/templates/layouts/base.gohtml; grep -v '^#' workspace/templates/layouts/base.gohtml | grep -c 'Live Editor Active' | grep -qx 1</automated>
  </verify>
  <done>ICE-01 badge and ICE-05 auth script wired per D-54, D-52, D-69.</done>
</task>

<task type="auto">
  <name>Task 2: index.gohtml hero binding and inline edit</name>
  <files>internal/scaffold/embed/index.gohtml, workspace/templates/index.gohtml</files>
  <action>
    Replace static hero h1/p with {{.Hero.Title}} and {{.Hero.Body}} per D-68. Wrap ALL edit attributes inside {{if .IsLoggedIn}} on title and body elements per D-63: contenteditable, hx-put /api/collections/hero_content/records/homepage, hx-trigger blur, hx-vals js partial updates, hx-ext json-enc on title only (D-66), a11y attrs per 03-UI-SPEC (role textbox, aria-label, spellcheck). Add hero__title--editable and hero__body--editable classes when logged in. Retain Phase 1 site header and Alpine mobile nav unchanged. Unauthenticated HTML must not contain substring contenteditable anywhere (ICE-06). Mirror to workspace/templates/index.gohtml.
  </action>
  <verify>
    <automated>grep -v '^#' workspace/templates/index.gohtml | grep -c 'hero_content/records/homepage' | grep -qx 2</automated>
  </verify>
  <done>ICE-03, ICE-04, DEMO-02 template markup complete.</done>
</task>

<task type="auto">
  <name>Task 3: main.css pulse and editable focus styles</name>
  <files>internal/scaffold/embed/main.css, workspace/assets/main.css</files>
  <action>
    Extend .editor-badge__dot with pulse ::after animation per 03-UI-SPEC CSS additions (editor-dot-ping keyframes). Add .hero__title--editable and .hero__body--editable with cursor text and :focus-visible indigo outline (2px, 2px offset). Add prefers-reduced-motion media query disabling dot animation per 03-UI-SPEC a11y. Do not change Phase 1 typography scale. Mirror to workspace/assets/main.css.
  </action>
  <verify>
    <automated>grep -v '^#' workspace/assets/main.css | grep -c 'hero__title--editable' | grep -qx 1</automated>
  </verify>
  <done>DEMO-03 editor overlay styles activated; ICE-06 visual cleanliness when logged out.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSR template → browser HTML | Edit surface must not leak to anonymous users |
| Inline script → HTMX requests | AuthToken visible in page source when logged in |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | contenteditable exposure | mitigate | {{if .IsLoggedIn}} wraps all edit attrs (D-63) |
| T-03-08 | Info disclosure | AuthToken in view-source | accept | Required for HTMX Bearer (D-52); short-lived JWT |
| T-03-09 | Repudiation | silent blur-save | accept | No toast UX deferred; PocketBase audit via API |
</threat_model>

<verification>
- monms validate on updated gohtml files
- grep logged-out template render has no contenteditable (covered in 03-04)
</verification>

<success_criteria>
- Badge renders only when IsLoggedIn
- Index hero uses dynamic Hero fields with conditional inline edit
- CSS pulse and focus styles match 03-UI-SPEC
</success_criteria>

<output>
After completion, create `.planning/phases/03-inline-contextual-editing-demonstration-content/03-03-SUMMARY.md`
</output>
