---
phase: "03"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - workspace/schema/hero_content.json
  - internal/scaffold/embed/hero_content.json
  - internal/scaffold/init.go
  - internal/schema/seed.go
  - internal/schema/sync.go
  - internal/schema/seed_test.go
autonomous: true
requirements:
  - DEMO-01
  - SEC-02

must_haves:
  truths:
    - "hero_content collection exists after bootstrap with title and body text fields"
    - "homepage record with id homepage is seeded idempotently on every bootstrap"
    - "Public list/view rules allow unauthenticated read; update requires authenticated user"
    - "Fresh monms init copies hero_content.json into workspace/schema/"
  artifacts:
    - path: "workspace/schema/hero_content.json"
      provides: "Declarative hero_content collection with API rules per D-57, D-61"
      contains: "hero_content"
    - path: "internal/schema/seed.go"
      provides: "seedHeroHomepage idempotent record seed per D-58"
      exports: ["seedHeroHomepage"]
    - path: "internal/schema/sync.go"
      provides: "Bootstrap hook calls seed after ImportCollectionsByMarshaledJSON"
      contains: "seedHeroHomepage"
  key_links:
    - from: "internal/schema/sync.go"
      to: "workspace/schema/hero_content.json"
      via: "loadSchemaJSONFiles merges JSON on bootstrap"
      pattern: "ImportCollectionsByMarshaledJSON"
    - from: "internal/schema/seed.go"
      to: "PocketBase app.Save"
      via: "record.Set id homepage then Save"
      pattern: "FindRecordById.*homepage"
---

<objective>
Add the hero_content PocketBase collection with SEC-02 API rules and idempotent homepage record seeding on bootstrap. Ship schema JSON in workspace and monms init scaffold so DEMO-01 works on fresh and existing workspaces.

Purpose: Inline editing and SSR hero load depend on a stable collection + fixed record id homepage (D-58, D-64).
Output: hero_content.json fixtures, seed.go, extended bootstrap hook, init scaffold copy.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-inline-contextual-editing-demonstration-content/03-CONTEXT.md
@.planning/phases/03-inline-contextual-editing-demonstration-content/03-RESEARCH.md
@workspace/schema/press_releases.json

<interfaces>
From internal/schema/sync.go — extend this hook, do not replace import logic:
```go
func RegisterBootstrapHook(app core.App, wsAbs string)
// OnBootstrap: e.Next() → loadSchemaJSONFiles → ImportCollectionsByMarshaledJSON → seedHeroHomepage
```

From workspace/schema/press_releases.json — JSON shape reference (hero_content adds *Rule fields).

Seed copy per 03-UI-SPEC.md Copywriting Contract:
- title: "Welcome to MonMS"
- body: "This headline and paragraph are stored in the hero_content collection. Sign in via Admin, then click here to edit in place — changes save when you click away."
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: hero_content schema JSON with API rules</name>
  <files>workspace/schema/hero_content.json, internal/scaffold/embed/hero_content.json</files>
  <behavior>
    - JSON has name hero_content, type base, fields title and body (text)
    - listRule and viewRule are empty strings (public read per D-61)
    - updateRule, createRule, deleteRule require @request.auth.id != ''
  </behavior>
  <action>
    Create workspace/schema/hero_content.json per D-57 and D-61 using RESEARCH Pattern 1 recommended rules (empty string for public list/view, not null). Mirror identical content to internal/scaffold/embed/hero_content.json for monms init. Match press_releases.json field array style. If PocketBase import rejects rule syntax, adjust per RESEARCH discretion and document in SUMMARY.
  </action>
  <verify>
    <automated>test -f workspace/schema/hero_content.json && grep -v '^#' workspace/schema/hero_content.json | grep -c '"name": "hero_content"' | grep -qx 1</automated>
  </verify>
  <done>DEMO-01 schema artifact exists with SEC-02 rule fields declared.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Idempotent homepage bootstrap seed</name>
  <files>internal/schema/seed.go, internal/schema/sync.go, internal/schema/seed_test.go</files>
  <behavior>
    - seedHeroHomepage returns nil when hero_content collection missing (no error)
    - seedHeroHomepage skips when homepage record already exists
    - seedHeroHomepage creates record id homepage with UI-SPEC seed title/body on first run
    - RegisterBootstrapHook calls seed after successful ImportCollectionsByMarshaledJSON
  </behavior>
  <action>
    Add internal/schema/seed.go with seedHeroHomepage(app core.App) error per D-58 and RESEARCH Pattern 2: FindCollectionByNameOrId hero_content, FindRecordById homepage short-circuit, core.NewRecord, record.Set id/title/body, app.Save. Extend RegisterBootstrapHook in sync.go to call seedHeroHomepage after import (even when raw import is empty — seed still runs if collection exists from prior bootstrap). Log seed failures with slog.Warn; do not fail bootstrap (D-60 handler fallback). Write seed_test.go using pocketbase.NewWithConfig temp dir + manual ImportCollections + double seed call asserting single homepage record.
  </action>
  <verify>
    <automated>go test ./internal/schema/ -run TestSeedHeroHomepage -count=1 -timeout 60s</automated>
  </verify>
  <done>homepage record seeded idempotently on bootstrap per D-58.</done>
</task>

<task type="auto">
  <name>Task 3: monms init ships hero_content schema</name>
  <files>internal/scaffold/init.go</files>
  <action>
    Extend RunInit to copy embed/hero_content.json to schema/hero_content.json using the same skip-if-exists pattern as other scaffold files (D-69). Do not force-overwrite existing operator workspaces. Add init_test assertion that fresh init creates schema/hero_content.json when absent.
  </action>
  <verify>
    <automated>go test ./internal/scaffold/ -run TestRunInit -count=1 -timeout 60s</automated>
  </verify>
  <done>Fresh monms init includes hero_content.json in workspace/schema/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public → PocketBase list/view | Unauthenticated read of hero_content records |
| Authenticated → PocketBase update | Bearer or session auth required for mutations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | hero_content PUT | mitigate | updateRule @request.auth.id != '' in schema JSON (D-61) |
| T-03-02 | Elevation | create/delete records | mitigate | createRule and deleteRule require auth (D-61) |
| T-03-03 | Info disclosure | public list/view | accept | Demo content intentionally public-read for SSR |
</threat_model>

<verification>
- go test ./internal/schema/... -count=1
- Bootstrap with test workspace imports hero_content and seeds homepage
</verification>

<success_criteria>
- hero_content collection + homepage record exist after app.Bootstrap()
- Seed is idempotent across repeated bootstrap
- SEC-02 rules encoded in schema JSON
</success_criteria>

<output>
After completion, create `.planning/phases/03-inline-contextual-editing-demonstration-content/03-01-SUMMARY.md`
</output>
