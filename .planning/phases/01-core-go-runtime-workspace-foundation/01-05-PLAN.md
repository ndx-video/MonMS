---
phase: 01-core-go-runtime-workspace-foundation
plan: 05
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - main.go
  - internal/scaffold/init.go
  - internal/scaffold/embed/base.gohtml
  - internal/scaffold/embed/index.gohtml
  - internal/scaffold/embed/errors.gohtml
  - internal/scaffold/embed/main.css
  - internal/scaffold/init_test.go
autonomous: true
requirements:
  - WRK-01
  - WRK-02
  - DEMO-03
must_haves:
  truths:
    - "monms init creates full workspace tree with base layout, index stub, errors template, main.css, schema/, fragments/"
    - "monms init runs git init only when .git absent"
    - "base.gohtml includes Tailwind CDN, HTMX 1.9.12, Alpine 3.14.8, hidden editor overlay per DEMO-03"
    - "Index stub Alpine mobile nav toggles without page reload"
  artifacts:
    - path: internal/scaffold/init.go
      provides: monms init command implementation
      exports: ["RunInit"]
    - path: internal/scaffold/embed/base.gohtml
      provides: Embedded base layout matching 01-UI-SPEC.md
    - path: workspace/templates/layouts/base.gohtml
      provides: Generated site base layout (after init)
      contains: "htmx.org@1.9.12"
  key_links:
    - from: main.go
      to: internal/scaffold/init.go
      via: early dispatch os.Args[1]==init
      pattern: "scaffold\\.RunInit"
    - from: internal/scaffold/init.go
      to: embed.FS
      via: Write scaffold files to resolved workspace
      pattern: "embed\\."
---

<objective>
Implement `monms init` with embedded scaffold templates and CSS that exactly match the Phase 1 UI contract — the operator-facing workspace bootstrap.

Purpose: D-05/D-07 require separate init; WRK-01/02 and DEMO-03 require directory structure and CDN wiring without Phase 3 auth UI.
Output: Runnable `go run . init` producing a Git-ready workspace operators can serve immediately.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-go-runtime-workspace-foundation/01-UI-SPEC.md
@.planning/phases/01-core-go-runtime-workspace-foundation/01-CONTEXT.md
@.planning/phases/01-core-go-runtime-workspace-foundation/01-PATTERNS.md
@specs/monms-prd.md

**UI contract is authoritative** for template markup, CDN order, copy, and main.css classes. PRD §5 auth badge and pb_auth script are omitted per D-24.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Embedded scaffold assets per UI-SPEC</name>
  <read_first>
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-UI-SPEC.md — entire document (CDN order, base layout, index stub, error page, main.css component classes, copywriting contract)
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-CONTEXT.md — D-21, D-22, D-23, D-24, D-07
    - specs/monms-prd.md §5 lines 235–314 — structure reference only; omit IsLoggedIn badge and pb_auth script
  </read_first>
  <files>
    internal/scaffold/embed/base.gohtml,
    internal/scaffold/embed/index.gohtml,
    internal/scaffold/embed/errors.gohtml,
    internal/scaffold/embed/main.css
  </files>
  <action>
    Create `internal/scaffold/embed/` with go:embed FS.
    `base.gohtml` per 01-UI-SPEC Base Layout Structure: CDN order (Tailwind Play, inline config, main.css link, HTMX 1.9.12 in head; Alpine 3.14.8 defer at body end); `{{define "base"}}` with `{{template "body" .}}`; `#editor-overlay` with class hidden and aria-hidden true (D-24); commented Phase 3 auth script block only; body classes bg-slate-50 text-slate-900 min-h-screen; main max-w-6xl mx-auto px-6 py-12.
    `index.gohtml`: site header with Alpine x-data mobile nav per UI-SPEC; hero copy "MonMS is running" and body text from copywriting contract; Explore Admin CTA to /_/; card section; NO PocketBase record reads; NO contenteditable; NO HTMX attributes on content (D-23).
    `errors.gohtml` per Error Page Styling with .Code, .Message, Path-aware 404 message from handler.
    `main.css`: all component classes from UI-SPEC (.site-header, .btn, .card, .hero, .error-page, .mobile-nav, [x-cloak], .editor-badge unused styles).
    Pin CDN versions exactly as UI-SPEC Registry table.
  </action>
  <acceptance_criteria>
    - base.gohtml contains unpkg.com/htmx.org@1.9.12 and alpinejs@3.14.8
    - base.gohtml does NOT contain Live Editor Active visible badge (overlay hidden)
    - index.gohtml contains x-data and mobile nav toggle
    - main.css defines .btn--primary and .hero__title
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -v '^#' internal/scaffold/embed/base.gohtml | grep -c 'htmx.org@1.9.12')" -eq 1 && test "$(grep -v '^#' internal/scaffold/embed/index.gohtml | grep -c 'x-data')" -ge 1</automated>
  </verify>
  <done>Embedded templates match 01-UI-SPEC.md byte-for-byte on structure and versions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: monms init command and git bootstrap</name>
  <read_first>
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-CONTEXT.md — D-05, D-07, D-08, D-28
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-RESEARCH.md — Pattern 1 RunInit
    - internal/config/config.go — ResolveWorkspace for init path
  </read_first>
  <files>internal/scaffold/init.go, main.go, internal/scaffold/init_test.go</files>
  <behavior>
    - RunInit creates templates/layouts/, templates/fragments/, templates/errors/, assets/, schema/
    - Writes all embed files to correct relative paths under workspace
    - Does not create .gitignore for .pb_data (D-28)
    - git init only when .git missing; skip when .git exists
    - Idempotent: second init does not overwrite existing files OR overwrites safely — prefer skip existing files with log unless force flag absent (no force flag in Phase 1: skip existing, log info)
  </behavior>
  <action>
    Implement `RunInit(args []string) error` using config.ResolveWorkspace.
    Write files: templates/layouts/base.gohtml, templates/index.gohtml, templates/errors/errors.gohtml, assets/main.css, schema/.gitkeep, templates/fragments/.gitkeep.
    If `.git` not present in workspace, run `git init` with workspace as cwd; on git missing from PATH log warning and continue (RESEARCH env table).
    Update main.go early dispatch to call real RunInit (remove stub from 01-02).
    TestInitScaffold: temp dir, RunInit, assert directories and files exist per WRK-01.
    TestInitGit: with/without .git scenarios.
  </action>
  <acceptance_criteria>
    - TestInitScaffold and TestInitGit pass
    - After init, workspace.ValidateWorkspace succeeds
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/scaffold/... -run 'TestInitScaffold|TestInitGit' -count=1</automated>
  </verify>
  <done>WRK-01 and WRK-02 satisfied; D-07 scaffold list complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: DEMO-03 CDN and layout verification test</name>
  <read_first>
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-UI-SPEC.md — Scaffold File Checklist
    - .planning/REQUIREMENTS.md — DEMO-03
  </read_first>
  <files>internal/scaffold/init_test.go</files>
  <behavior>
    - After RunInit, base.gohtml on disk contains htmx.org, alpinejs, cdn.tailwindcss.com, /assets/main.css link
    - index.gohtml contains hero__title class
  </behavior>
  <action>
    Implement TestBaseLayoutCDN reading written workspace files from TestInitScaffold fixture.
    Assert required substrings per behavior; fail with clear missing-token message.
  </action>
  <acceptance_criteria>
    - TestBaseLayoutCDN passes
    - DEMO-03 traceability documented in test name comment
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/scaffold/... -run TestBaseLayoutCDN -count=1</automated>
  </verify>
  <done>DEMO-03 automated; init produces serve-ready workspace.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → monms init | Writes files to filesystem path from flag/env |
| CDN scripts in base.gohtml | Third-party script load in browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Tampering | init writes | mitigate | Write only under resolved workspace abs path; never follow symlinks out of workspace (use filepath.Join under ws root) |
| T-01-13 | Tampering | CDN scripts | transfer | UI-SPEC pins versions; SRI not required for Play CDN per spec — accept supply-chain risk for zero-build constraint |
| T-01-14 | Repudiation | git init | accept | Operator owns Git config; init only creates repo |

</threat_model>

<verification>
- `go test ./internal/scaffold/... -count=1`
- Manual smoke: `go run . init && go run . serve` then browser GET /
</verification>

<success_criteria>
- `monms init` produces workspace passing ValidateWorkspace
- All UI-SPEC scaffold files present with correct CDN pins
- WRK-01, WRK-02, DEMO-03 tests green
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-go-runtime-workspace-foundation/01-05-SUMMARY.md`
</output>
