---
phase: 01-core-go-runtime-workspace-foundation
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - main.go
  - internal/schema/sync.go
  - internal/templates/cache.go
  - internal/templates/cache_test.go
autonomous: true
requirements:
  - ENG-01
  - SEC-01
must_haves:
  truths:
    - "monms serve starts PocketBase with data dir at workspace/.pb_data/"
    - "PocketBase admin dashboard responds at /_/"
    - "Startup logs configured and absolute workspace paths plus buildMode"
    - "Declarative schema/*.json syncs collections on bootstrap when present"
  artifacts:
    - path: main.go
      provides: Early init dispatch, runServe, OnBootstrap, OnServe registration stub
      exports: ["main", "buildMode"]
    - path: internal/schema/sync.go
      provides: Schema JSON load and ImportCollectionsByMarshaledJSON
      exports: ["RegisterBootstrapHook"]
    - path: internal/templates/cache.go
      provides: TemplateCache with production gate from buildMode
      exports: ["TemplateCache", "NewCache"]
  key_links:
    - from: main.go
      to: workspace/.pb_data
      via: pocketbase.Config.DefaultDataDir
      pattern: "DefaultDataDir.*\\.pb_data"
    - from: internal/schema/sync.go
      to: workspace/schema/*.json
      via: OnBootstrap after e.Next()
      pattern: "ImportCollectionsByMarshaledJSON"
---

<objective>
Wire the PocketBase embedded server: early `init` dispatch, workspace-scoped SQLite data dir, schema sync on bootstrap, and minimal serve path that proves HTTP + admin routes work.

Purpose: ENG-01 and SEC-01 require a running binary without external config files; D-27 and D-32 require co-located pb_data and declarative schema import.
Output: Compilable `main.go` with `runServe()`, schema bootstrap hook, and global `TemplateCache` instance for later router plans.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-go-runtime-workspace-foundation/01-01-SUMMARY.md
@.planning/phases/01-core-go-runtime-workspace-foundation/01-CONTEXT.md
@.planning/phases/01-core-go-runtime-workspace-foundation/01-RESEARCH.md
@.planning/phases/01-core-go-runtime-workspace-foundation/01-PATTERNS.md
@specs/monms-prd.md

<interfaces>
From internal/config/config.go (plan 01-01):
```go
func ResolveWorkspace(args []string, env []string) (configured string, absolute string, err error)
```

From internal/workspace/validate.go (plan 01-01):
```go
func ValidateWorkspace(ws string) error
```

PocketBase bootstrap (RESEARCH Pattern 2):
```go
app := pocketbase.NewWithConfig(pocketbase.Config{
    DefaultDataDir: filepath.Join(wsAbs, ".pb_data"),
})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: main.go CLI dispatch and PocketBase serve bootstrap</name>
  <read_first>
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-CONTEXT.md — D-01, D-04, D-05, D-06, D-27, D-31
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-RESEARCH.md — Pattern 1 Early-Dispatch CLI, Pattern 2 NewWithConfig
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-PATTERNS.md — main.go adaptation notes
    - specs/monms-prd.md §3 lines 75–118 — reference only; do not copy ENV check
  </read_first>
  <files>main.go</files>
  <action>
    Replace stub main with full entry per D-05/D-06:
    - If `len(os.Args) >= 2 && os.Args[1] == "init"`, call `scaffold.RunInit(os.Args[2:])` — stub in plan 01-05: create `internal/scaffold/init.go` with `func RunInit(args []string) error { return fmt.Errorf("monms init not implemented yet") }` only if needed to compile; prefer leaving init call commented with TODO until 01-05 OR add minimal stub package.
    - Otherwise call `runServe()`.
    `runServe()` flow: `config.ResolveWorkspace` → `workspace.ValidateWorkspace` (fatal stderr + exit 1 on error per D-06) → `slog.Info("workspace configured", "path", configured, "absolute", abs, "mode", buildMode)` per D-31 → `pocketbase.NewWithConfig` with `DefaultDataDir: filepath.Join(abs, ".pb_data")` per D-27.
    Register `schema.RegisterBootstrapHook(app, abs)` before `app.Start()`.
    Register `app.OnServe()` that only calls `se.Next()` for now (routes added in plan 01-04).
    Do NOT start fsnotify here (plan 01-03); do NOT auto-scaffold workspace (D-05).
    Align PocketBase `DefaultDev` with buildMode: set `DefaultDev: buildMode != "production"` in Config when field exists.
  </action>
  <acceptance_criteria>
    - `go run . serve` with valid testutil workspace exits non-zero without routes OR starts listening (PocketBase default port)
    - Missing workspace causes exit 1 and message containing `monms init`
    - `buildMode` remains package-level var overridable via `-ldflags "-X main.buildMode=production"`
    - No `os.Getenv("ENV")` production detection
  </acceptance_criteria>
  <verify>
    <automated>go build -o /dev/null .</automated>
  </verify>
  <done>Binary boots PocketBase with workspace-scoped `.pb_data`; validation and logging active.</done>
</task>

<task type="auto">
  <name>Task 2: TemplateCache skeleton and schema bootstrap hook</name>
  <read_first>
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-CONTEXT.md — D-01, D-02, D-03, D-32
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-RESEARCH.md — Pattern 5 Schema Sync, TemplateCache example
    - specs/monms-prd.md §3 TemplateCache struct lines 62–73
  </read_first>
  <files>internal/templates/cache.go, internal/templates/cache_test.go, internal/schema/sync.go</files>
  <action>
    Implement `internal/templates/cache.go`:
    - `type TemplateCache struct { mu sync.RWMutex; cache map[string]*template.Template }`
    - `func NewCache() *TemplateCache`
    - `func (c *TemplateCache) Active() bool` returns `buildMode == "production"` — import buildMode via setter `func (c *TemplateCache) SetProductionMode(prod bool)` set from main at startup OR duplicate `var productionMode` set by `main` calling `cache.SetProductionMode(buildMode == "production")` to avoid importing main from internal package.
    - `func (c *TemplateCache) Get(key string, loader func() (*template.Template, error)) (*template.Template, error)` per RESEARCH: RLock check, loader on miss, Lock store only if Active().
    - `func (c *TemplateCache) Flush()` replaces cache map.
    Implement `internal/schema/sync.go`:
    - `func RegisterBootstrapHook(app core.App, wsAbs string)` binds `app.OnBootstrap()` after `e.Next()`.
    - `loadSchemaJSONFiles(dir string) ([]byte, error)` reads `schema/*.json` in sorted filename order; each file may be one collection object or array; merge to single JSON array; skip invalid files with `slog.Error` log-and-continue per RESEARCH discretion.
    - If merged array empty, return nil without calling import (Pitfall 6).
    - Else `e.App.ImportCollectionsByMarshaledJSON(raw, false)`.
    Replace skips in `cache_test.go` with passing unit tests (no t.Skip): `TestDevNoCache` uses fake loader counting invocations — Active() false means two Get calls invoke loader twice; `TestCacheFlush` — Active() true caches first Get, Flush clears, second Get invokes loader again.
  </action>
  <acceptance_criteria>
    - `TemplateCache.Active()` false when production mode not set; true when SetProductionMode(true)
    - Empty `schema/` directory does not error on bootstrap
    - Invalid JSON file logs error and does not prevent serve start
    - TestCacheFlush and TestDevNoCache pass without skip
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/templates/... -run 'TestCacheFlush|TestDevNoCache' -count=1 -short</automated>
  </verify>
  <done>Cache and schema packages ready for router and watcher plans.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove serve starts and admin route exists</name>
  <read_first>
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-VALIDATION.md — TestServeStarts, TestAdminDashboard
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-RESEARCH.md — ENG-01, SEC-01
  </read_first>
  <files>internal/router/handlers_test.go</files>
  <behavior>
    - TestServeStarts: with temp workspace from testutil, start app on ephemeral port (or use httptest against PB test harness), GET `/api/health` or root returns non-5xx
    - TestAdminDashboard: GET `/_/` returns 200 or 302 to admin (PocketBase built-in)
  </behavior>
  <action>
    Replace skips in `TestServeStarts` and `TestAdminDashboard` with integration tests using testutil workspace and short timeout. Use PocketBase test patterns: start app, defer stop. Workspace must pass ValidateWorkspace.
    Do not register custom SSR routes yet — only verify PocketBase core routes.
  </action>
  <acceptance_criteria>
    - TestServeStarts passes without manual server
    - TestAdminDashboard passes — admin path reachable (SEC-01)
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/router/... -run 'TestServeStarts|TestAdminDashboard' -count=1 -timeout 120s</automated>
  </verify>
  <done>ENG-01 and SEC-01 automated smoke tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace/schema/*.json → SQLite | Untrusted declarative schema from Git workspace |
| HTTP → PocketBase admin /_/ | Operator-facing admin surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | schema/sync.go | mitigate | Log-and-continue on invalid JSON; never execute code from schema files; import only via PocketBase API |
| T-01-06 | Elevation | PocketBase admin /_/ | accept | SEC-01 requires admin accessible; network exposure is deployment concern — document bind to localhost in README (out of plan scope) |
| T-01-07 | Denial of service | ImportCollections on huge schema | accept | Phase 1: sorted file load; no size cap — operator trust model |

</threat_model>

<verification>
- `go test ./internal/router/... -run 'TestServeStarts|TestAdminDashboard' -count=1`
- `go test ./internal/templates/... -count=1 -short`
</verification>

<success_criteria>
- PocketBase serves with `workspace/.pb_data` per D-27
- Schema sync runs on bootstrap per D-32
- ENG-01 and SEC-01 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-go-runtime-workspace-foundation/01-02-SUMMARY.md`
</output>
