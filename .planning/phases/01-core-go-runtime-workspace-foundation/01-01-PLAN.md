---
phase: 01-core-go-runtime-workspace-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - main.go
  - internal/config/config.go
  - internal/workspace/validate.go
  - internal/testutil/workspace.go
  - internal/templates/cache_test.go
  - internal/templates/resolver_test.go
  - internal/templates/watcher_test.go
  - internal/router/handlers_test.go
  - internal/router/perf_test.go
  - internal/scaffold/init_test.go
autonomous: true
requirements:
  - WRK-01
must_haves:
  truths:
    - "go test ./... -short runs without 'no test files' or missing package errors"
    - "Workspace path resolves from --workspace flag, MONMS_WORKSPACE env, or ./workspace default"
    - "Incomplete workspace fails validation with monms init instruction"
  artifacts:
    - path: go.mod
      provides: Go module with pocketbase v0.38.1 and fsnotify v1.9.0
      contains: "github.com/pocketbase/pocketbase"
    - path: internal/testutil/workspace.go
      provides: Temp workspace fixture for handler and scaffold tests
    - path: internal/config/config.go
      provides: ResolveWorkspace with flag-over-env precedence
      exports: ["ResolveWorkspace"]
    - path: internal/workspace/validate.go
      provides: Structural workspace validation before serve
      exports: ["ValidateWorkspace"]
  key_links:
    - from: internal/scaffold/init_test.go
      to: internal/testutil/workspace.go
      via: import and fixture setup
      pattern: "testutil\\."
    - from: internal/workspace/validate.go
      to: templates/layouts/base.gohtml
      via: os.Stat check
      pattern: "templates/layouts/base.gohtml"
---

<objective>
Initialize the Go module, workspace path resolution, validation helpers, and the full test scaffold required by 01-VALIDATION.md Wave 0 before any feature implementation.

Purpose: Nyquist rule — every downstream plan task must have an `<automated>` command target; Wave 0 creates those targets.
Output: `go.mod`, config/validate packages, testutil fixture, and skeleton `*_test.go` files with real test names from the validation map.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-core-go-runtime-workspace-foundation/01-CONTEXT.md
@.planning/phases/01-core-go-runtime-workspace-foundation/01-RESEARCH.md
@.planning/phases/01-core-go-runtime-workspace-foundation/01-VALIDATION.md
@.planning/phases/01-core-go-runtime-workspace-foundation/01-PATTERNS.md

**Module path:** `github.com/monms/monms` (greenfield; no existing go.mod).

**Locked decisions this plan implements:** D-25, D-26, D-06, D-09.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Go module and package skeleton</name>
  <read_first>
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-RESEARCH.md — Standard Stack table (PocketBase v0.38.1, fsnotify v1.9.0)
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-RESEARCH.md — Recommended Project Structure
    - specs/monms-prd.md §3 — import list reference only
  </read_first>
  <files>go.mod, go.sum, main.go</files>
  <action>
    Run `go mod init github.com/monms/monms` at repo root.
    Run `go get github.com/pocketbase/pocketbase@v0.38.1` and `go get github.com/fsnotify/fsnotify@v1.9.0`.
    Create `main.go` with only: package main; `var buildMode = "development"` (D-01); empty `func main() {}` so the module compiles — full bootstrap lands in plan 01-02.
    Do not add ENV-based production detection (ROADMAP ENV toggle is superseded by D-01).
  </action>
  <acceptance_criteria>
    - `go.mod` module path is `github.com/monms/monms`
    - `go.mod` requires `github.com/pocketbase/pocketbase v0.38.1` and `github.com/fsnotify/fsnotify v1.9.0`
    - `main.go` declares `var buildMode = "development"` and compiles
  </acceptance_criteria>
  <verify>
    <automated>go build -o /dev/null . && go mod verify</automated>
  </verify>
  <done>Module initializes; `go build .` succeeds with stub main.</done>
</task>

<task type="auto">
  <name>Task 2: Workspace config, validation, and testutil fixture</name>
  <read_first>
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-CONTEXT.md — D-25, D-26, D-06, D-09
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-PATTERNS.md — internal/config/config.go and internal/workspace/validate.go patterns
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-RESEARCH.md — Workspace Validation snippet
  </read_first>
  <files>internal/config/config.go, internal/workspace/validate.go, internal/testutil/workspace.go</files>
  <action>
    Implement `config.ResolveWorkspace(args []string, env []string) (configured string, absolute string, err error)` per D-26: default `./workspace`; parse `--workspace=/path` and `--workspace /path` from args; if flag absent use `MONMS_WORKSPACE`; flag wins over env; return configured path for logging (D-31) and absolute path for file operations.
    Implement `workspace.ValidateWorkspace(ws string) error` per D-09: require `filepath.Join(ws, "templates/layouts/base.gohtml")` and `filepath.Join(ws, "assets")` exist; error message must include `Run: monms init` and imply exit code 1 (D-06).
    Implement `testutil.NewWorkspace(t *testing.T) string` that creates a temp dir with minimal valid structure: `templates/layouts/base.gohtml` (minimal `{{define "base"}}...{{template "body" .}}...{{end}}`), `assets/main.css` (empty or `/* test */`), `templates/index.gohtml` with `{{define "body"}}ok{{end}}`, `schema/` directory.
    Export packages as `github.com/monms/monms/internal/config`, `internal/workspace`, `internal/testutil`.
  </action>
  <acceptance_criteria>
    - `ResolveWorkspace([]string{"--workspace", "/tmp/ws"}, nil)` returns configured `/tmp/ws` and absolute path under `/tmp/ws`
    - With `MONMS_WORKSPACE=/env/ws` and no flag, configured path is `/env/ws`
    - `ValidateWorkspace` returns non-nil error when `base.gohtml` missing; error text contains `monms init`
    - `testutil.NewWorkspace` returns a directory passing `ValidateWorkspace`
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/config/... ./internal/workspace/... -count=1 -short</automated>
  </verify>
  <done>Config and validation packages tested; testutil fixture usable by Wave 1+ tests.</done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 test scaffolds per validation map</name>
  <read_first>
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-VALIDATION.md — Wave 0 Requirements and Per-Task Verification Map
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-RESEARCH.md — Phase Requirements → Test Map
  </read_first>
  <files>
    internal/templates/cache_test.go,
    internal/templates/resolver_test.go,
    internal/templates/watcher_test.go,
    internal/router/handlers_test.go,
    internal/router/perf_test.go,
    internal/scaffold/init_test.go
  </files>
  <action>
    Create skeleton test files with package names matching directories. Each file must declare at least one test function matching 01-VALIDATION.md names (body may be `t.Skip("implemented in plan 01-XX")` until the owning plan lands):
    - `cache_test.go`: `TestCacheFlush`, `TestDevNoCache`
    - `resolver_test.go`: `TestResolveSlug` (table cases: `/` → index, `press` → press/index, `press/2024` → press/2024.gohtml per D-10)
    - `watcher_test.go`: `TestWatcherInvalidates`
    - `handlers_test.go`: `TestServeStarts`, `TestAssetsHandler`, `Test404NoPanic`, `TestAdminDashboard`
    - `perf_test.go`: `TestIdleMemory`, `TestTTFB`
    - `init_test.go`: `TestInitScaffold`, `TestInitGit`, `TestBaseLayoutCDN`
    Add `internal/testutil/buildmode.go` helper: `func WithProduction(t *testing.T, fn func())` documented for plans using ldflags — can set env or build tag stub; production cache tests use `t.Setenv` only if insufficient — prefer documenting `-ldflags "-X main.buildMode=production"` in test comments for plan 01-02.
    Implement fully now: `TestInitScaffold` skeleton that only asserts `testutil.NewWorkspace` creates dirs `schema`, `templates/fragments`, `assets` (WRK-01 partial) OR skip with clear message — prefer minimal real test: after plan 01-05, init will populate; for Wave 0 run `TestResolveSlug` as unit test against `resolver` package once resolver stub exists. If resolver not yet implemented, use `t.Skip` in resolver tests only; config/workspace tests must pass.
    Ensure `go test ./... -short -count=1` exits 0 (skips allowed, no compile errors).
  </action>
  <acceptance_criteria>
    - All seven test file paths exist under `internal/`
    - `go test ./... -short -count=1` completes with exit code 0
    - Each validation-map test name exists as a function in the correct package
  </acceptance_criteria>
  <verify>
    <automated>go test ./... -short -count=1</automated>
  </verify>
  <done>Wave 0 test infrastructure installed; downstream plans replace skips with real assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args / env → workspace path | Untrusted operator input selects filesystem root |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Tampering | config.ResolveWorkspace | mitigate | Reject empty path after resolution; use filepath.Clean on configured path before Abs |
| T-01-04 | Info disclosure | workspace.ValidateWorkspace | accept | Error messages reveal missing paths only — acceptable for operator CLI |

</threat_model>

<verification>
- `go test ./... -short -count=1` green
- `go mod verify` passes
- Source audit: WRK-01 directory structure validated by testutil fixture (full init scaffold in plan 01-05)
</verification>

<success_criteria>
- Go module and dependencies pinned per RESEARCH.md
- Config and validation implement D-25, D-26, D-09
- All Wave 0 test files exist with named tests from 01-VALIDATION.md
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-go-runtime-workspace-foundation/01-01-SUMMARY.md`
</output>
