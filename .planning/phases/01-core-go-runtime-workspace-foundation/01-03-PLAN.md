---
phase: 01-core-go-runtime-workspace-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - main.go
  - internal/templates/cache.go
  - internal/templates/resolver.go
  - internal/templates/watcher.go
  - internal/templates/cache_test.go
  - internal/templates/resolver_test.go
  - internal/templates/watcher_test.go
autonomous: true
requirements:
  - ENG-02
  - ENG-03
  - ENG-04
must_haves:
  truths:
    - "Production mode caches parsed templates on first access per slug"
    - "Development mode re-reads templates from disk every request"
    - "Production fsnotify on .gohtml changes flushes entire cache within debounce window"
    - "URL /press resolves to templates/press/index.gohtml and / to templates/index.gohtml"
  artifacts:
    - path: internal/templates/resolver.go
      provides: Mirror+index slug resolution
      exports: ["ResolveSlug", "ErrNotFound"]
    - path: internal/templates/watcher.go
      provides: Recursive debounced workspace watch
      exports: ["StartWatcher"]
    - path: internal/templates/cache.go
      provides: Full Get/Flush/Active implementation
  key_links:
    - from: main.go
      to: internal/templates/watcher.go
      via: StartWatcher only when buildMode production
      pattern: "StartWatcher"
    - from: internal/templates/watcher.go
      to: internal/templates/cache.go
      via: onChange callback Flush()
      pattern: "Flush\\(\\)"
---

<objective>
Implement template caching, production-only fsnotify invalidation, and nested slug resolution — the core runtime malleability layer.

Purpose: ENG-02/03/04 and WRK-04 depend on correct cache semantics and resolver before SSR handlers render pages.
Output: Complete `internal/templates` package with passing unit/integration tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-go-runtime-workspace-foundation/01-02-SUMMARY.md
@.planning/phases/01-core-go-runtime-workspace-foundation/01-CONTEXT.md
@.planning/phases/01-core-go-runtime-workspace-foundation/01-RESEARCH.md
@specs/monms-prd.md

<interfaces>
From internal/templates/cache.go (plan 01-02):
```go
type TemplateCache struct { /* mu, cache */ }
func NewCache() *TemplateCache
func (c *TemplateCache) SetProductionMode(prod bool)
func (c *TemplateCache) Active() bool
func (c *TemplateCache) Get(key string, loader func() (*template.Template, error)) (*template.Template, error)
func (c *TemplateCache) Flush()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Slug resolver mirror+index algorithm</name>
  <read_first>
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-CONTEXT.md — D-10, D-11, D-12, D-13
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-RESEARCH.md — Pattern 3 Template Resolution table
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-PATTERNS.md — internal/templates/resolver.go
  </read_first>
  <files>internal/templates/resolver.go, internal/templates/resolver_test.go</files>
  <behavior>
    - ResolveSlug(ws, "") → templates/index.gohtml
    - ResolveSlug(ws, "press") → templates/press/index.gohtml when that file exists
    - ResolveSlug(ws, "press/2024") → templates/press/2024.gohtml
    - Trailing slash stripped: "about/" same as "about"
    - Flat file wins: if both templates/about.gohtml and templates/about/index.gohtml exist, return flat path
    - Missing template returns ErrNotFound
  </behavior>
  <action>
    Implement `ResolveSlug(wsAbs, slug string) (pagePath string, err error)` per algorithm in RESEARCH Pattern 3. Use `filepath.Join(wsAbs, "templates", ...)`. Preserve slug case (D-13). Export `var ErrNotFound = errors.New("template not found")`.
    Replace `TestResolveSlug` skip with table-driven tests using testutil workspace layouts.
  </action>
  <acceptance_criteria>
    - All behavior cases in `<behavior>` pass in TestResolveSlug
    - Resolver never panics on missing files
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/templates/... -run TestResolveSlug -count=1</automated>
  </verify>
  <done>WRK-04 resolution logic complete and tested.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Template cache dev vs production semantics</name>
  <read_first>
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-CONTEXT.md — D-02, D-03
    - specs/monms-prd.md §3 getTemplate lines 120–145
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-RESEARCH.md — TemplateCache example
  </read_first>
  <files>internal/templates/cache.go, internal/templates/cache_test.go</files>
  <behavior>
    - With Active() false, two Get calls with loader increment loader call count (disk every time)
    - With Active() true, second Get with same key does not call loader again
    - Flush() causes next Get to call loader again
  </behavior>
  <action>
    Finish cache.go if stubbed: loader must parse layout+page paths supplied by caller (loader closure from router plan). Tests use fake loader counting invocations.
    Implement TestCacheFlush and TestDevNoCache per behavior block.
  </action>
  <acceptance_criteria>
    - TestDevNoCache and TestCacheFlush pass
    - Active() strictly tied to SetProductionMode, not ENV
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/templates/... -run 'TestCacheFlush|TestDevNoCache' -count=1</automated>
  </verify>
  <done>ENG-02 and ENG-04 cache behavior verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Production fsnotify watcher and main wiring</name>
  <read_first>
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-CONTEXT.md — D-04, D-30
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-RESEARCH.md — Pattern 6 fsnotify recursive debounce
    - specs/monms-prd.md §3 watchWorkspace — adapt, do not copy templates-only path
  </read_first>
  <files>internal/templates/watcher.go, internal/templates/watcher_test.go, main.go</files>
  <behavior>
    - Watcher not started when production mode false
    - Writing a .gohtml file under workspace triggers Flush within 200ms (debounce 100ms + margin)
    - Creating new template subdirectory gets added to watcher
    - Non-.gohtml files do not trigger flush
  </behavior>
  <action>
    Implement `StartWatcher(ctx context.Context, wsRoot string, onChange func()) error` per RESEARCH debounce example: WalkDir add all dirs; on Create dir re-Add; filter Write/Create on `.gohtml`; 100ms debounce per path calling onChange once.
    In `main.go` `runServe()`, after cache created: if `buildMode == "production"` start watcher goroutine with `onChange: tplCache.Flush` (D-04). Pass context cancelled on shutdown if PB exposes signal; else background ctx.
    TestWatcherInvalidates: temp dir, production cache, write file, assert Flush effect via loader count.
  </action>
  <acceptance_criteria>
    - TestWatcherInvalidates passes on Linux
    - Dev mode: no fsnotify goroutine (verify via test or buildMode check assertion)
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/templates/... -run TestWatcherInvalidates -count=1 -timeout 30s</automated>
  </verify>
  <done>ENG-03 satisfied via D-30 whole-workspace .gohtml watch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace filesystem → in-memory cache | Agent-written templates affect server memory |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Denial of service | watcher debounce | mitigate | 100ms debounce per path prevents flush storms on bulk git checkout |
| T-01-09 | Tampering | cached templates | accept | Cache reflects workspace disk — operator trusts Git workspace; integrity is Git's job in Phase 1 |

</threat_model>

<verification>
- `go test ./internal/templates/... -count=1`
- Production build: `go build -ldflags "-X main.buildMode=production" -o /tmp/monms .` succeeds
</verification>

<success_criteria>
- Resolver implements D-10, D-11, D-12
- Cache implements D-02, D-03
- Watcher implements D-04, D-30 with passing TestWatcherInvalidates
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-go-runtime-workspace-foundation/01-03-SUMMARY.md`
</output>
