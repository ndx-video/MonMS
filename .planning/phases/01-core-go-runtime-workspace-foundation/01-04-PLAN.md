---
phase: 01-core-go-runtime-workspace-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-02", "01-03", "01-05"]
files_modified:
  - main.go
  - internal/router/ssr.go
  - internal/router/assets.go
  - internal/router/fragments.go
  - internal/router/handlers_test.go
  - internal/router/perf_test.go
autonomous: true
requirements:
  - ENG-05
  - ENG-06
  - WRK-03
  - WRK-04
  - WRK-05
must_haves:
  truths:
    - "GET /assets/main.css returns CSS from workspace/assets with traversal blocked"
    - "GET /fragments/{name} returns partial HTML without base layout"
    - "GET /unknown returns styled 404 with attempted path, no panic"
    - "Template parse errors return 500 with detail in dev, generic in production"
    - "SSR catch-all does not handle /api, /_/, /assets paths"
  artifacts:
    - path: internal/router/assets.go
      provides: Root-jailed static file handler
      exports: ["AssetsHandler"]
    - path: internal/router/fragments.go
      provides: HTMX fragment partial handler
      exports: ["FragmentsHandler"]
    - path: internal/router/ssr.go
      provides: Catch-all SSR with error pages
      exports: ["SSRHandler", "RegisterRoutes"]
  key_links:
    - from: internal/router/ssr.go
      to: internal/templates/resolver.go
      via: ResolveSlug before cache Get
      pattern: "ResolveSlug"
    - from: internal/router/ssr.go
      to: internal/templates/cache.go
      via: Get(slug, loader) then ExecuteTemplate base
      pattern: "ExecuteTemplate.*base"
    - from: main.go
      to: internal/router
      via: OnServe route order assets → fragments → slug
      pattern: "GET\\(\"/assets"
---

<objective>
Register HTTP handlers for static assets, HTMX fragments, and SSR pages — including styled error responses and performance gates.

Purpose: WRK-03/04/05 and D-14–D-20 require the user-visible routing layer; ENG-05/ENG-06 validated here.
Output: Full public HTTP surface for Phase 1 content (excluding Phase 3 auth UI).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-core-go-runtime-workspace-foundation/01-03-SUMMARY.md
@.planning/phases/01-core-go-runtime-workspace-foundation/01-UI-SPEC.md
@.planning/phases/01-core-go-runtime-workspace-foundation/01-CONTEXT.md
@specs/monms-prd.md

<interfaces>
```go
// internal/templates/resolver.go
func ResolveSlug(wsAbs, slug string) (pagePath string, err error)

// internal/templates/cache.go
func (c *TemplateCache) Get(key string, loader func() (*template.Template, error)) (*template.Template, error)

// PocketBase RequestEvent
func (e *RequestEvent) File(path string) error
func (e *RequestEvent) JSON(status int, data any) error
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Static assets handler with path jail</name>
  <read_first>
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-CONTEXT.md — D-29, WRK-03
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-RESEARCH.md — safeAssetPath, Pitfall 5
    - specs/monms-prd.md §3 assets route lines 83–86
  </read_first>
  <files>internal/router/assets.go, internal/router/handlers_test.go</files>
  <behavior>
    - GET /assets/main.css returns 200 with text/css when file exists in fixture
    - GET /assets/../../../etc/passwd returns 403
    - GET /assets/missing.css returns 404
  </behavior>
  <action>
    Implement `AssetsHandler(wsAbs string) func(*core.RequestEvent) error` using `e.Request.PathValue("path")`.
    Implement `safeAssetPath(assetsRoot, requestPath string) (string, error)` with filepath.Clean, Abs, and prefix check per RESEARCH — reject with 403 before e.File.
    Register in `RegisterRoutes` as first route: `GET /assets/{path...}`.
    Implement TestAssetsHandler with httptest against app from plan 01-02 pattern.
  </action>
  <acceptance_criteria>
    - TestAssetsHandler passes all behavior cases
    - No path traversal succeeds in tests
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/router/... -run TestAssetsHandler -count=1 -timeout 60s</automated>
  </verify>
  <done>WRK-03 complete with D-29 jail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SSR catch-all, fragments, and error pages</name>
  <read_first>
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-CONTEXT.md — D-10 through D-20, D-14, D-15, D-16
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-UI-SPEC.md — Error Page Styling, Template context
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-PATTERNS.md — ssr.go, fragments.go
    - specs/monms-prd.md §3 SSR handler — use ExecuteTemplate not Execute
  </read_first>
  <files>internal/router/ssr.go, internal/router/fragments.go, internal/router/handlers_test.go, main.go</files>
  <behavior>
    - TestHomepageSSR: GET / returns 200 HTML containing hero title text from index stub when workspace scaffold present
    - GET /does-not-exist returns 404 HTML containing "Page not found: /does-not-exist"
    - Missing template never panics
    - GET /fragments/{name} returns partial without html wrapper when fragment exists
    - Production parse error: 500 body does not contain raw Go error string
    - Dev parse error: 500 body may contain parse detail
  </behavior>
  <action>
    Implement `SSRHandler(wsAbs string, cache *templates.TemplateCache, isDev bool)`.
    Slug from `e.Request.PathValue("slug")` — empty slug passes "" to ResolveSlug (D-11).
    Loader: parse `layouts/base.gohtml` + resolved page; return via cache.Get(slug, loader).
    Execute `tmpl.ExecuteTemplate(e.Response, "base", data)` with data map: IsLoggedIn false, Slug, Path for errors.
    On ErrNotFound: renderError 404 via templates/errors/errors.gohtml + base with Code 404, Message `Page not found: /{path}` (D-19), Path field for template.
    On parse error: renderError 500 — dev shows err.Error(), prod shows generic copy per 01-UI-SPEC (D-16, D-20).
    If errors.gohtml missing: minimal built-in HTML per D-18 and UI-SPEC copywriting contract.
    Implement `FragmentsHandler`: parse `templates/fragments/{name}.gohtml` only, Execute (not base), 404 if missing (D-15).
    Implement `RegisterRoutes(se *core.ServeEvent, deps)` registering assets, fragments, then `GET /{slug...}` last.
    Wire `main.go` OnServe to call RegisterRoutes.
    SSR handler must not run for paths starting with api/, _/, assets/ — rely on route registration order (D-14); add defensive check in handler if slug starts with reserved prefix return 404.
    TestHomepageSSR: httptest GET / against app with init-scaffolded or testutil workspace; assert 200 and body contains hero title substring from index stub (e.g. "MonMS is running" or `.hero__title` content).
    Test404NoPanic: assert no panic, 404 status, body contains path.
  </action>
  <acceptance_criteria>
    - TestHomepageSSR and Test404NoPanic pass
    - ExecuteTemplate always uses "base" for full pages
    - Fragment responses lack DOCTYPE from base layout
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/router/... -run 'TestHomepageSSR|Test404NoPanic' -count=1 -timeout 60s</automated>
  </verify>
  <done>WRK-05 and D-17–D-20 error UX complete.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Performance gates ENG-05 and ENG-06</name>
  <read_first>
    - .planning/PROJECT.md — RAM under 30MB, TTFB under 15ms
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-RESEARCH.md — ENG-05/06 validation, Phase-Specific #5 static index
    - .planning/phases/01-core-go-runtime-workspace-foundation/01-VALIDATION.md — TestIdleMemory, TestTTFB
  </read_first>
  <files>internal/router/perf_test.go</files>
  <behavior>
    - After GC, runtime.ReadMemStats heap alloc under 30*1024*1024 bytes with app started in production mode (short-lived test server)
    - httptest GET / completes in under 15ms median over 10 requests with production cache warm (static index, no DB)
  </behavior>
  <action>
    Implement TestIdleMemory: start server with testutil workspace, production mode on cache, force GC, read MemStats, assert threshold with reasonable skip if CI environment exceeds (document in test log, do not weaken threshold below 30MB).
    Implement TestTTFB: warm cache with one request, then measure 10 GET / — assert p50 under 15ms.
    Use static index template without PocketBase record reads per RESEARCH.
  </action>
  <acceptance_criteria>
    - Tests run without -short skip unless documented CI flake
    - Thresholds match PROJECT.md targets
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/router/... -run 'TestIdleMemory|TestTTFB' -count=1 -timeout 120s</automated>
  </verify>
  <done>ENG-05 and ENG-06 have automated gates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → GET /assets/{path} | Untrusted path segment |
| Client → GET /{slug...} | Untrusted URL slug |
| Template content → HTML response | XSS if text/template misused |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering / Info disclosure | assets.go | mitigate | D-29 root-jail with Abs+prefix; return 403 on traversal |
| T-01-02 | Info disclosure | ssr.go 500 responses | mitigate | D-16: generic production message; no stack traces in prod |
| T-01-10 | Tampering | ssr.go slug | mitigate | html/template auto-escaping; use html/template not text/template; map values escaped in templates |
| T-01-11 | Spoofing | SSR catch-all | mitigate | D-14: register assets/fragments before catch-all; defensive reserved-prefix check |

</threat_model>

<verification>
- `go test ./internal/router/... -count=1 -timeout 120s`
- Manual: `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8090/assets/main.css` after init+serve
</verification>

<success_criteria>
- All router tests green including perf gates
- WRK-03, WRK-04, WRK-05 satisfied end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-go-runtime-workspace-foundation/01-04-SUMMARY.md`
</output>
