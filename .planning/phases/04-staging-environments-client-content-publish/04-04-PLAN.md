---
phase: "04"
plan: "04"
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - internal/content/auth.go
  - internal/content/routes.go
  - internal/content/routes_test.go
  - main.go
autonomous: true
user_setup:
  - service: monms-production
    why: "Production import API requires deploy-time secret"
    env_vars:
      - name: MONMS_PUBLISH_TOKEN
        source: "Generate long random string; set on production instance only"
requirements:
  - PUB-05
must_haves:
  truths:
    - "POST /api/monms/content/import returns 401 without valid Bearer token"
    - "POST with valid MONMS_PUBLISH_TOKEN upserts editorial records idempotently"
    - "Import rejects non-editorial collection names in payload"
    - "Empty MONMS_PUBLISH_TOKEN causes import route to fail closed (401)"
  artifacts:
    - path: internal/content/auth.go
      provides: "RequirePublishToken middleware per PUB-05"
      exports: ["RequirePublishToken"]
    - path: internal/content/routes.go
      provides: "RegisterRoutes with POST /api/monms/content/import"
      exports: ["RegisterRoutes", "Deps"]
    - path: main.go
      provides: "OnServe registers content routes before router.RegisterRoutes"
      contains: "content.RegisterRoutes"
  key_links:
    - from: internal/content/routes.go
      to: internal/content/import.go
      via: ImportPayload handler
      pattern: "ImportPayload"
    - from: internal/content/auth.go
      to: POST /api/monms/content/import
      via: RequirePublishToken Bind middleware
      pattern: "ConstantTimeCompare"
---

<objective>
Expose production content import API with scoped publish token auth — not superuser JWT — upserting only editorial collections.

Purpose: PUB-05 and D-51 content rail endpoint on production; staging publish (plan 04-05) POSTs here.
Output: auth middleware, import route, integration tests, main.go OnServe wiring.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-staging-environments-client-content-publish/04-02-SUMMARY.md
@.planning/phases/04-staging-environments-client-content-publish/04-RESEARCH.md
@internal/router/handlers_test.go
@main.go

<interfaces>
From internal/content/import.go (plan 04-02):
```go
func ImportPayload(app core.App, collections []CollectionPayload, editorialNames []string) (ImportReport, error)
```

Request body (specs/staging.md §5.4):
```json
{"collections":[{"name":"hero_content","records":[{"id":"homepage",...}]}]}
```

From internal/config/config.go:
```go
func envValue(env []string, key string) string
```

RegisterRoutes Deps (04-PATTERNS):
```go
type Deps struct {
    WsAbs        string
    PublishToken string
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish token middleware and import handler</name>
  <read_first>
    - .planning/phases/04-staging-environments-client-content-publish/04-RESEARCH.md — Pattern 4 RequirePublishToken
    - .planning/phases/04-staging-environments-client-content-publish/04-PATTERNS.md — auth.go and routes.go
    - workspace/SECURITY.md — never log tokens
  </read_first>
  <files>internal/content/auth.go, internal/content/routes.go, internal/content/routes_test.go</files>
  <action>
    Implement auth.go RequirePublishToken(expected string): if expected empty, middleware always returns UnauthorizedError (fail closed per RESEARCH Environment Availability). Use crypto/subtle.ConstantTimeCompare on Bearer token after strings.CutPrefix.
    Implement routes.go RegisterRoutes(se, deps): POST /api/monms/content/import with Bind(RequirePublishToken(deps.PublishToken)); BindBody into importRequest; load editorial names from deps.WsAbs/schema; call ImportPayload; return JSON ImportReport with counts and warnings. Deny hardcoded system names (_superusers, users) even if mistakenly marked editorial.
    routes_test.go TestImportAPIUnauthorized: no Authorization → status >= 400; wrong token → 401; valid test-publish-token → 200 and record upserted. Use startTestServerWithApp pattern from handlers_test.go extended with content.RegisterRoutes in OnServe before RegisterRoutes. RegisterAuthHooks not required for token route.
  </action>
  <acceptance_criteria>
    - Missing Authorization header returns unauthorized
    - Valid Bearer matching PublishToken returns 200
    - Payload with collection not in editorial allowlist rejected
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/content/ -run ImportAPI -count=1 -timeout 120s</automated>
  </verify>
  <done>PUB-05 production import API with scoped token auth implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Wire RegisterRoutes in main.go OnServe</name>
  <read_first>
    - main.go lines 77–84 — existing OnServe hook
    - CLAUDE.md D-14 route order — /api/monms/* before SSR catch-all
  </read_first>
  <files>main.go</files>
  <action>
    Extend app.OnServe BindFunc: call content.RegisterRoutes(se, content.Deps{WsAbs: abs, PublishToken: config.EnvValue or os.Getenv("MONMS_PUBLISH_TOKEN")}) BEFORE router.RegisterRoutes per D-14 and RESEARCH. Use config package env lookup consistent with rest of codebase. Do not register publish UI routes here — staging-only handlers land in plan 04-05 same RegisterRoutes file.
  </action>
  <acceptance_criteria>
    - main.go compiles with content import on OnServe
    - Import route registered in same hook as SSR routes
  </acceptance_criteria>
  <verify>
    <automated>go build -o /dev/null . && go test ./internal/content/ -run ImportAPI -count=1 -timeout 60s</automated>
  </verify>
  <done>Production import API live when monms serve runs with MONMS_PUBLISH_TOKEN set.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → POST /api/monms/content/import | Untrusted callers with publish token can upsert editorial records |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Elevation | stolen publish token | mitigate | Long random token; env-only; rotate on compromise; editorial-only allowlist |
| T-04-09 | Tampering | import auth users | mitigate | Reject system collection names; editorial allowlist from schema parse |
| T-04-10 | Info disclosure | token in slog | mitigate | Never log MONMS_PUBLISH_TOKEN |
| T-04-11 | Denial of service | large import body | mitigate | Return 400 on excessive record count (cap e.g. 1000 records per collection in handler) |
</threat_model>

<verification>
- go test ./internal/content/ -run ImportAPI -count=1
- go test ./... -count=1 -short
</verification>

<success_criteria>
- PUB-05 satisfied: scoped token import API on production instance
- Fail closed when token unset
</success_criteria>

<output>
After completion, create `.planning/phases/04-staging-environments-client-content-publish/04-04-SUMMARY.md`
</output>
