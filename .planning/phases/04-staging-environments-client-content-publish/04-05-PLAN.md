---
phase: "04"
plan: "05"
type: execute
wave: 4
depends_on: ["04-03", "04-04"]
files_modified:
  - internal/content/routes.go
  - internal/content/auth.go
  - internal/content/publish.go
  - internal/content/embed/publish.gohtml
  - internal/content/routes_test.go
  - workspace/templates/layouts/base.gohtml
  - internal/scaffold/embed/base.gohtml
autonomous: true
user_setup:
  - service: monms-staging
    why: "Staging publish UI needs production URL and publisher allowlist"
    env_vars:
      - name: MONMS_PUBLISH_TOKEN
        source: "Same token value configured on production (outbound from staging publish POST)"
    dashboard_config:
      - task: "Copy workspace/.monms/config.example.json to workspace/.monms/config.json"
        location: "Staging workspace — set productionUrl and publisherEmails"
requirements:
  - PUB-06
  - PUB-07
  - PUB-08
must_haves:
  truths:
    - "GET /api/monms/publish returns HTML publish console for superuser publisher"
    - "Page shows last published timestamp and unpublished changes status from publish-state.json"
    - "POST /api/monms/publish exports and forwards to production import then updates checksum"
    - "Non-publisher superuser gets 403 on POST publish but can still inline edit"
    - "Editor badge links Publish to live for publisher allowlist emails only"
  artifacts:
    - path: internal/content/embed/publish.gohtml
      provides: "Publish console UI per 04-UI-SPEC.md"
    - path: internal/content/routes.go
      provides: "GET/POST /api/monms/publish and GET /api/monms/publish/diff"
      contains: "/api/monms/publish"
    - path: workspace/templates/layouts/base.gohtml
      provides: "Publish to live link in editor badge per PUB-06"
      contains: "/api/monms/publish"
  key_links:
    - from: POST /api/monms/publish
      to: production /api/monms/content/import
      via: PublishToProduction from publish.go
      pattern: "PublishToProduction"
    - from: GET /api/monms/publish
      to: internal/content/diff.go
      via: DiffExport for pending changes display
      pattern: "DiffExport"
    - from: POST /api/monms/publish success
      to: workspace/.monms/publish-state.json
      via: WritePublishState with new checksum
      pattern: "WritePublishState"
---

<objective>
Deliver client-facing Publish to live staging console with diff preview, publisher gating, checksum update, and editor badge link — primary UX per D-53.

Purpose: PUB-06 admin publish flow; PUB-07 publisher vs editor; PUB-08 unpublished indicator in UI.
Output: HTML publish page, JSON diff endpoint, POST publish handler, base.gohtml badge extension, integration tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-staging-environments-client-content-publish/04-03-SUMMARY.md
@.planning/phases/04-staging-environments-client-content-publish/04-04-SUMMARY.md
@.planning/phases/04-staging-environments-client-content-publish/04-UI-SPEC.md
@internal/router/auth.go
@internal/router/inline_edit_test.go

<interfaces>
From 04-UI-SPEC.md — page layout, copy, error states, accessibility (status text + color dot).

From internal/content (prior plans):
```go
func DiffExport(app core.App, wsAbs string) (DiffResult, error)
func WritePublishState(wsAbs string, state PublishState) error
func ReadPublishState(wsAbs string) (PublishState, error)
func PublishToProduction(productionURL, token string, payload []CollectionPayload) error
```

Publisher config (workspace/.monms/config.json):
```json
{"productionUrl":"https://production.example.com","publisherEmails":["publisher@client.com"]}
```

RequirePublisher from 04-PATTERNS — check e.Auth email in allowlist after apis.RequireSuperuserAuth().
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publisher gate and staging publish routes</name>
  <read_first>
    - .planning/phases/04-staging-environments-client-content-publish/04-RESEARCH.md — Pattern 5 and 6
    - .planning/phases/04-staging-environments-client-content-publish/04-UI-SPEC.md — Auth and error states
    - internal/router/inline_edit_test.go — superuser test client pattern
  </read_first>
  <files>internal/content/auth.go, internal/content/routes.go, internal/content/routes_test.go</files>
  <action>
    Extend auth.go: LoadMonmsConfig(wsAbs) reads .monms/config.json (fallback empty); RequirePublisher(allowedEmails) after superuser check; IsPublisher(email, list) helper for template/badge use in plan task 2.
    Extend routes.go RegisterRoutes: GET /api/monms/publish — Bind apis.RequireSuperuserAuth() then RequirePublisher; render publish.gohtml with LastPublished, HasChanges, DiffSummary, ProductionURLSet bool. GET /api/monms/publish/diff — JSON DiffResult for HTMX/fetch. POST /api/monms/publish — RequirePublisher; read productionUrl from config and MONMS_PUBLISH_TOKEN from env; ExportAll/build payload; PublishToProduction; on success WritePublishState with ChecksumExport of payload and PublishedAt time.Now UTC; on production error return message without updating state (04-UI-SPEC error state).
    If productionUrl empty: render setup instructions referencing config.example.json (no POST).
    routes_test.go TestPublishUIReturns200: publisher superuser GET /api/monms/publish → 200 HTML contains "Publish to live". TestPublisherGate: editor superuser not in list POST → 403; publisher POST with httptest mock production → 200 and publish-state updated.
  </action>
  <acceptance_criteria>
    - Publisher GET publish page 200 with title Publish to live
    - Non-publisher POST returns 403
    - Successful POST writes publish-state.json checksum
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/content/ -run 'PublishUI|PublisherGate' -count=1 -timeout 120s</automated>
  </verify>
  <done>PUB-06 routes and PUB-07 publisher gate implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Publish HTML template per UI-SPEC</name>
  <read_first>
    - .planning/phases/04-staging-environments-client-content-publish/04-UI-SPEC.md — full layout, components, copywriting, a11y
    - workspace/assets/main.css — editor-badge classes from Phase 3
  </read_first>
  <files>internal/content/embed/publish.gohtml, internal/content/routes.go</files>
  <action>
    Create embed/publish.gohtml per 04-UI-SPEC: header with Back to / link; last published RFC3339; status badge with text label (Up to date / Unpublished changes) not color-only; diff list as ul/dl field changes; form POST Publish now button type submit; Preview diff link to GET /api/monms/publish/diff or inline section; error/success message blocks; setup state when ProductionURLSet false.
    Parse with html/template embed; execute in GET handler with Content-Type text/html charset utf-8. Reuse main.css via link /assets/main.css in template head for slate palette consistency.
    Minimal vanilla JS optional for diff refresh — not required if server renders diff on GET.
  </action>
  <acceptance_criteria>
    - HTML includes Publish now button and status text labels per UI-SPEC copy table
    - Diff section uses semantic ul or dl
    - Page loads main.css
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/content/ -run PublishUI -count=1 -timeout 60s</automated>
  </verify>
  <done>PUB-06 diff preview UI rendered server-side per 04-UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 3: Editor badge Publish link for publishers</name>
  <read_first>
    - workspace/templates/layouts/base.gohtml — editor badge block lines 19–24
    - .planning/phases/04-staging-environments-client-content-publish/04-PATTERNS.md — base.gohtml extension
    - internal/router/ssr.go — template data IsLoggedIn AuthToken
  </read_first>
  <files>workspace/templates/layouts/base.gohtml, internal/scaffold/embed/base.gohtml, internal/router/ssr.go</files>
  <action>
    Extend SSR template data with IsPublisher bool: load .monms/config.json publisherEmails when IsLoggedIn; set true when session email in list (reuse auth record email from existing auth context). Per 04-UI-SPEC prefer hide link for non-publishers.
    Add to editor badge in both base.gohtml copies: when IsPublisher show anchor href="/api/monms/publish" class editor-badge__link text "Publish to live" before Full Admin Dashboard link.
    Dual-write workspace and internal/scaffold/embed base.gohtml per CLAUDE.md.
  </action>
  <acceptance_criteria>
    - Publisher session renders Publish to live link in badge
    - Non-publisher logged-in user does not see publish link
    - Link targets /api/monms/publish not /_/publish
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/router/ -run 'EditorBadge|PublishLink' -count=1 -timeout 120s 2>/dev/null || grep -q 'Publish to live' workspace/templates/layouts/base.gohtml internal/scaffold/embed/base.gohtml</automated>
  </verify>
  <done>PUB-06 discoverability via editor badge; PUB-08 status visible on publish page.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Staging superuser → POST /api/monms/publish | Publisher allowlist gates live promotion |
| Staging → production HTTPS | Publish token on outbound import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Elevation | editor triggers live publish | mitigate | RequirePublisher email allowlist on POST (PUB-07) |
| T-04-13 | Tampering | publish without review | mitigate | Diff preview on GET before POST; checksum state only updates on success |
| T-04-14 | Repudiation | denied publish attempt | accept | slog.Info publish attempts with email; no audit DB in Phase 4 |
</threat_model>

<verification>
- go test ./internal/content/... ./internal/router/... -count=1 -short
- Manual: login as publisher → /api/monms/publish shows diff
</verification>

<success_criteria>
- Client Publish to live flow works end-to-end against mock production
- Publisher vs editor separation enforced
- Last-publish checksum updated only after successful import
</success_criteria>

<output>
After completion, create `.planning/phases/04-staging-environments-client-content-publish/04-05-SUMMARY.md`
</output>
