---
phase: "04"
plan: "06"
type: execute
wave: 5
depends_on: ["04-05"]
files_modified:
  - README.md
  - workspace/README.md
  - workspace/MEDIA.md
  - workspace/EDITING-GUIDE.md
  - CLAUDE.md
  - workspace/.gitignore
autonomous: true
requirements:
  - ENV-01
  - ENV-02
  - ENV-03
  - MED-02
must_haves:
  truths:
    - "README and workspace README document four layers L1–L4 with distinct promotion rails"
    - "Docs state structure promotes via Git tags; content via JSON upsert independently"
    - "Docs state staging and production are separate instances with separate .pb_data/"
    - "MEDIA.md warns against PocketBase-local file storage for publishable assets"
    - ".gitignore excludes .monms/config.json and publish-state.json"
  artifacts:
    - path: workspace/MEDIA.md
      provides: "CDN URL policy and file-field warning per MED-02"
    - path: workspace/README.md
      provides: "Four-layer table and dual-rail lifecycle per ENV-01/02/03"
      contains: "L3"
    - path: README.md
      provides: "monms content CLI and publish flow cross-links"
    - path: workspace/.gitignore
      provides: "Ignores .pb_data/, .monms/config.json, publish-state.json"
  key_links:
    - from: workspace/README.md
      to: specs/staging.md
      via: cross-link accepted spec
      pattern: "specs/staging.md"
    - from: workspace/EDITING-GUIDE.md
      to: /api/monms/publish
      via: Publish to live section
      pattern: "Publish to live"
---

<objective>
Document four-layer lifecycle, dual promotion rails, separate environments, and CDN media policy so operators and clients can deploy staging/production without consultant involvement in routine content pushes.

Purpose: ENV-01–03 and MED-02 are documentation requirements; complements implemented code from plans 04-01–04-05.
Output: MEDIA.md, README updates, EDITING-GUIDE publish section, gitignore, CLAUDE.md cross-links.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@specs/staging.md
@.planning/phases/04-staging-environments-client-content-publish/04-CONTEXT.md
@workspace/README.md
@CLAUDE.md
@.planning/phases/03-inline-contextual-editing-demonstration-content/03-04-SUMMARY.md

**Do not duplicate specs/staging.md** — cross-link and summarize operator steps only.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Four-layer and dual-rail documentation</name>
  <read_first>
    - specs/staging.md §2–4 — four layers and two-rail model
    - workspace/README.md — existing four-layer table lines 7–14
    - .planning/REQUIREMENTS.md — ENV-01, ENV-02, ENV-03
  </read_first>
  <files>README.md, workspace/README.md, CLAUDE.md</files>
  <action>
    Update workspace/README.md: expand four-layer table (L1 engine, L2 structure Git, L3 content .pb_data + content/, L4 audience) per ENV-01 and D-50; add section Structure rail vs Content rail per ENV-02/D-51 — Git tag for templates/schema/assets; JSON upsert for editorial records; rails independent with note structure must lead new fields. Add Staging vs Production per ENV-03/D-52 — separate MonMS instances, separate .pb_data/, same binary version; staging edits L3, production serves L4; configure productionUrl and MONMS_PUBLISH_TOKEN once.
    Update root README.md: add v2 section summarizing monms content subcommands (export, import, diff, publish) and POST /api/monms/content/import; link specs/staging.md and workspace/README.md.
    Update CLAUDE.md Four layers section: mark Phase 4 implemented items (content rail, publish UI at /api/monms/publish not /_/publish per research); add monms content to Commands block; note .monms/ config gitignore.
  </action>
  <acceptance_criteria>
    - workspace README mentions Git tag structure promotion and separate content rail
    - workspace README states separate .pb_data per environment
    - README lists monms content subcommands
  </acceptance_criteria>
  <verify>
    <automated>grep -q 'Git tag' workspace/README.md && grep -q '\.pb_data' workspace/README.md && grep -q 'monms content' README.md</automated>
  </verify>
  <done>ENV-01, ENV-02, ENV-03 documented in README artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: MEDIA.md and publish operator guide</name>
  <read_first>
    - specs/staging.md §6 — media policy
    - .planning/phases/04-staging-environments-client-content-publish/04-PATTERNS.md — MEDIA.md content contract
    - workspace/EDITING-GUIDE.md — existing inline edit flow
  </read_first>
  <files>workspace/MEDIA.md, workspace/EDITING-GUIDE.md</files>
  <action>
    Create workspace/MEDIA.md per MED-02 and 04-PATTERNS: publishable media = public CDN URLs in text/HTML fields; do not use PocketBase file fields for cross-environment publishable assets; export skips file-type columns with warning; consultant one-time CDN bucket setup; link to specs/staging.md §6.
    Extend EDITING-GUIDE.md with Publish to live section: login as publisher; open /api/monms/publish from badge or direct URL; review diff; Publish now; explain editors edit on staging but cannot publish without allowlist (PUB-07); reference workspace/.monms/config.example.json for setup.
  </action>
  <acceptance_criteria>
    - MEDIA.md exists and warns against local file storage for publishable assets
    - EDITING-GUIDE mentions Publish to live and publisher role
  </acceptance_criteria>
  <verify>
    <automated>test -f workspace/MEDIA.md && grep -qi 'CDN' workspace/MEDIA.md && grep -q 'Publish to live' workspace/EDITING-GUIDE.md</automated>
  </verify>
  <done>MED-02 and client publish workflow documented.</done>
</task>

<task type="auto">
  <name>Task 3: Workspace gitignore for secrets and publish state</name>
  <read_first>
    - workspace/SECURITY.md — .pb_data gitignore guidance
    - .planning/phases/04-staging-environments-client-content-publish/04-RESEARCH.md — Pitfall 7 publish state in Git
  </read_first>
  <files>workspace/.gitignore</files>
  <action>
    Create or update workspace/.gitignore with: .pb_data/, .monms/config.json, .monms/publish-state.json. Optionally gitignore content/ with comment that commit is site policy (staging.md optional in Git) — default ignore content/ on staging per RESEARCH recommendation A5. Keep .monms/config.example.json committed. Document in workspace/README.md gitignore section.
  </action>
  <acceptance_criteria>
    - .gitignore excludes .pb_data and .monms secrets/state
    - config.example.json not ignored
  </acceptance_criteria>
  <verify>
    <automated>grep -q '\.pb_data' workspace/.gitignore && grep -q 'publish-state' workspace/.gitignore</automated>
  </verify>
  <done>Operational secrets and environment-specific publish state excluded from structure Git.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git-tracked docs → operators | Documentation must not embed live tokens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-15 | Info disclosure | tokens in committed config | mitigate | gitignore .monms/config.json; example file uses placeholders only |
</threat_model>

<verification>
- grep verification commands in tasks pass
- Docs cross-link specs/staging.md without contradicting D-50–D-56
</verification>

<success_criteria>
- All ENV and MED requirements have corresponding doc artifacts
- Operators can configure staging/production from README + MEDIA.md + EDITING-GUIDE
</success_criteria>

<output>
After completion, create `.planning/phases/04-staging-environments-client-content-publish/04-06-SUMMARY.md`
</output>
