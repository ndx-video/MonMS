---
phase: "04"
plan: "02"
type: execute
wave: 1
depends_on: ["04-01"]
files_modified:
  - internal/content/schema.go
  - internal/content/export.go
  - internal/content/import.go
  - internal/content/checksum.go
  - internal/content/diff.go
  - internal/content/state.go
  - internal/content/export_test.go
  - internal/content/import_test.go
  - workspace/content/hero_content.json
autonomous: true
requirements:
  - PUB-01
  - PUB-02
  - PUB-04
  - PUB-08
  - PUB-09
  - MED-01
must_haves:
  truths:
    - "ExportAll writes workspace/content/{collection}.json with stable record IDs"
    - "ImportFiles upserts records idempotently by ID (second import updates, no duplicates)"
    - "Diff detects title change between live export and publish-state checksum"
    - "Export skips file-type schema fields with slog.Warn per MED-01"
    - "publish-state.json stores sha256 checksum and publishedAt timestamp"
  artifacts:
    - path: internal/content/export.go
      provides: "ExportAll, ExportCollection → workspace/content/*.json per PUB-02"
      exports: ["ExportAll", "ExportCollection"]
    - path: internal/content/import.go
      provides: "ImportFiles, UpsertRecord, ImportPayload per PUB-04"
      exports: ["ImportFiles", "UpsertRecord", "ImportPayload"]
    - path: internal/content/state.go
      provides: "ReadPublishState, WritePublishState per PUB-08"
      exports: ["ReadPublishState", "WritePublishState"]
    - path: internal/content/diff.go
      provides: "DiffExport vs state per PUB-09"
      exports: ["DiffExport"]
    - path: workspace/content/hero_content.json
      provides: "Example export shape per specs/staging.md §5.1"
      contains: '"collection": "hero_content"'
  key_links:
    - from: internal/content/export.go
      to: internal/schema/editorial.go
      via: LoadEditorialCollectionNames filters collections
      pattern: "LoadEditorialCollectionNames"
    - from: internal/content/import.go
      to: PocketBase app.Save
      via: FindRecordById → NewRecord → Set fields → Save
      pattern: "UpsertRecord"
    - from: internal/content/state.go
      to: workspace/.monms/publish-state.json
      via: ensureUnderWorkspace path guard
      pattern: "publish-state.json"
---

<objective>
Implement the core content rail engine: export editorial records to JSON, idempotent upsert import, canonical checksum diff, and publish-state tracking — reusing seed.go upsert patterns.

Purpose: Content promotion rail (D-51, D-54) depends on stable JSON snapshots and upsert-by-ID before CLI or HTTP layers ship.
Output: internal/content package core (no CLI/routes yet), tests, example hero_content.json export file.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-staging-environments-client-content-publish/04-01-SUMMARY.md
@.planning/phases/04-staging-environments-client-content-publish/04-RESEARCH.md
@.planning/phases/04-staging-environments-client-content-publish/04-PATTERNS.md
@internal/schema/seed.go
@internal/schema/editorial.go
@specs/staging.md

<interfaces>
From internal/schema/seed.go — upsert pattern:
```go
func seedHeroHomepage(app core.App) error
// FindRecordById → NewRecord → Set → Save
```

From internal/schema/editorial.go (plan 04-01):
```go
func LoadEditorialCollectionNames(dir string) ([]string, error)
```

Content file shape (specs/staging.md §5.1):
```json
{"collection":"hero_content","records":[{"id":"homepage","title":"...","body":"..."}]}
```

Publish state shape (RESEARCH Pattern 7):
```json
{"checksum":"sha256:...","publishedAt":"2026-05-23T12:00:00Z","collections":["hero_content"]}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export and import with editorial allowlist</name>
  <read_first>
    - .planning/phases/04-staging-environments-client-content-publish/04-PATTERNS.md — export.go and import.go sections
    - internal/schema/seed.go — upsert lines 19–44
    - specs/staging.md §5.1–5.4 — content JSON shape and structure lag warnings
  </read_first>
  <files>internal/content/schema.go, internal/content/export.go, internal/content/import.go, internal/content/export_test.go, internal/content/import_test.go, workspace/content/hero_content.json</files>
  <action>
    Create internal/content/schema.go wrapping schema.LoadEditorialCollectionNames(filepath.Join(wsAbs,"schema")).
    Implement export.go: type CollectionFile {Collection string; Records []map[string]any}; ExportCollection uses FindAllRecords, PublicExport, strips collectionId/collectionName/expand; skip fields where coll.Fields.GetByName(k).Type() is file with slog.Warn per MED-01; ExportAll iterates editorial names, writes workspace/content/{name}.json with path guard from validate.go pattern; mkdir content if needed.
    Implement import.go: UpsertRecord mirrors 04-PATTERNS UpsertRecord; ImportFiles reads content/*.json with path guard; ImportPayload accepts []CollectionPayload for HTTP reuse in plan 04-04 — reject collection names not in editorial allowlist; skip unknown fields with slog.Warn per staging.md §5.4.
    Tests: bootstrap ephemeral app with testutil.NewEditorialWorkspace; seed homepage; ExportAll writes hero_content.json; ImportFiles twice leaves exactly one homepage record with updated title; assert JSON shape has collection and records keys.
    Add workspace/content/hero_content.json as committed example matching current seed copy (documentation artifact per PUB-02).
  </action>
  <acceptance_criteria>
    - Export produces {collection, records[]} with id homepage
    - Double ImportFiles is idempotent (one record, updated fields)
    - Non-editorial collection name in ImportPayload returns error
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/content/ -run 'Export|Import' -count=1 -timeout 120s</automated>
  </verify>
  <done>PUB-02 and PUB-04 core upsert/export implemented; MED-01 file fields skipped on export.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Checksum, diff, and publish state</name>
  <read_first>
    - .planning/phases/04-staging-environments-client-content-publish/04-RESEARCH.md — Pattern 7 checksum; PUB-08/PUB-09
    - .planning/phases/04-staging-environments-client-content-publish/04-PATTERNS.md — checksum.go, diff.go, state.go
    - internal/scaffold/init.go — ensureUnderWorkspace pattern
  </read_first>
  <files>internal/content/checksum.go, internal/content/diff.go, internal/content/state.go</files>
  <action>
    Implement checksum.go: ChecksumExport(payload any) returns sha256:hex from json.Marshal of sorted stable structure (sort collection names and record IDs before marshal for determinism per RESEARCH).
    Implement state.go: PublishState struct with Checksum, PublishedAt (RFC3339), Collections []string; ReadPublishState/WritePublishState under workspace/.monms/publish-state.json with ensureUnderWorkspace path guard; missing file returns zero state not error.
    Implement diff.go: DiffExport(app, wsAbs) exports current editorial snapshot, loads publish state, returns DiffResult with HasChanges bool and human-readable field deltas (collection/record/field old→new); DiffExport returns empty changes when checksum matches.
    Add tests in export_test.go or new diff_test.go: change homepage title after WritePublishState with old checksum → DiffExport HasChanges true and mentions title; matching checksum → HasChanges false.
  </action>
  <acceptance_criteria>
    - Checksum is stable for same content
    - Diff detects title field change
    - WritePublishState creates .monms/publish-state.json under workspace only
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/content/ -run 'Checksum|Diff|State' -count=1 -timeout 120s</automated>
  </verify>
  <done>PUB-08 state file and PUB-09 diff logic implemented.</done>
</task>

<task type="auto">
  <name>Task 3: File-field skip integration test (MED-01)</name>
  <read_first>
    - .planning/phases/04-staging-environments-client-content-publish/04-RESEARCH.md — Pitfall 2 file fields
    - MED-01 requirement in REQUIREMENTS.md
  </read_first>
  <files>internal/content/export_test.go</files>
  <action>
    Add TestExportSkipsFileFields: temp workspace with editorial collection schema containing one file field and one text field; create record with both; ExportCollection output map must not contain file field key; test may use minimal inline schema JSON in temp dir. Document in test comment that CDN URLs belong in text fields per D-55.
  </action>
  <acceptance_criteria>
    - Exported record map excludes file-type column
    - Text/URL fields still exported
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/content/ -run FileField -count=1 -timeout 60s</automated>
  </verify>
  <done>MED-01 export behavior verified: blobs not serialized, URL text fields preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workspace/content/*.json → ImportFiles | File contents upsert into SQLite via PocketBase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | path traversal in content file paths | mitigate | filepath.Rel guard on all reads/writes under wsAbs |
| T-04-04 | Tampering | import non-editorial collection | mitigate | Allowlist from LoadEditorialCollectionNames; deny system collection names |
| T-04-05 | Denial of service | oversized content JSON | accept | Phase 4 uses PocketBase default body limits; cap per-collection record count in plan 04-04 if needed |
</threat_model>

<verification>
- go test ./internal/content/... -count=1 -short
- Export/import round-trip on hero_content in temp workspace
</verification>

<success_criteria>
- Editorial export/import/diff/state pipeline works without HTTP or CLI
- All PUB-02, PUB-04, PUB-08, PUB-09, MED-01 behaviors test-covered
</success_criteria>

<output>
After completion, create `.planning/phases/04-staging-environments-client-content-publish/04-02-SUMMARY.md`
</output>
