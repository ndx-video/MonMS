---
phase: "04"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - internal/schema/editorial.go
  - internal/schema/editorial_test.go
  - internal/testutil/content.go
  - workspace/schema/hero_content.json
  - internal/scaffold/embed/hero_content.json
  - workspace/.monms/config.example.json
autonomous: true
requirements:
  - PUB-01
must_haves:
  truths:
    - "LoadEditorialCollectionNames returns hero_content when schema JSON has editorial true"
    - "Editorial flag is read from raw schema files, not live PocketBase Collection model"
    - "Test fixtures provide editorial workspace with .monms config template"
    - "hero_content schema JSON declares editorial true in workspace and scaffold embed"
  artifacts:
    - path: internal/schema/editorial.go
      provides: "Parse editorial flag from workspace/schema/*.json per D-54"
      exports: ["LoadEditorialCollectionNames", "SchemaMeta"]
    - path: internal/testutil/content.go
      provides: "NewEditorialWorkspace test helper"
      exports: ["NewEditorialWorkspace"]
    - path: workspace/.monms/config.example.json
      provides: "Committed staging config template without secrets"
    - path: workspace/schema/hero_content.json
      provides: "hero_content with editorial true per PUB-01"
      contains: '"editorial": true'
  key_links:
    - from: internal/schema/editorial.go
      to: workspace/schema/*.json
      via: ReadDir + json.Unmarshal SchemaMeta
      pattern: "LoadEditorialCollectionNames"
    - from: internal/testutil/content.go
      to: workspace/.monms/config.example.json
      via: fixture writes publisherEmails and productionUrl
      pattern: "publisher@test.local"
---

<objective>
Establish editorial collection discovery from raw schema JSON and Wave 0 test fixtures so downstream content export/import plans have deterministic editorial allowlists and test harnesses.

Purpose: PocketBase ImportCollections strips unknown keys like editorial (D-54); MonMS must parse schema files directly (RESEARCH Pitfall 1).
Output: editorial.go parser, editorial tests, testutil/content.go, hero_content editorial flag, config.example.json.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-staging-environments-client-content-publish/04-CONTEXT.md
@.planning/phases/04-staging-environments-client-content-publish/04-RESEARCH.md
@.planning/phases/04-staging-environments-client-content-publish/04-PATTERNS.md
@internal/schema/sync.go
@internal/schema/seed.go

<interfaces>
From internal/schema/sync.go — mirror loadSchemaJSONFiles dir scan (ReadDir, sort, trimJSON, continue-on-error):
```go
func loadSchemaJSONFiles(dir string) ([][]byte, error)
func trimJSON(data []byte) []byte
```

From 04-PATTERNS.md — SchemaMeta contract:
```go
type SchemaMeta struct {
    Name      string `json:"name"`
    Editorial bool   `json:"editorial"`
}
func LoadEditorialCollectionNames(dir string) ([]string, error)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Editorial flag parser from schema JSON</name>
  <read_first>
    - .planning/phases/04-staging-environments-client-content-publish/04-RESEARCH.md — Pattern 1 editorial flag; Pitfall 1
    - .planning/phases/04-staging-environments-client-content-publish/04-PATTERNS.md — internal/schema/editorial.go section
    - internal/schema/sync.go — loadSchemaJSONFiles lines 39–90
  </read_first>
  <files>internal/schema/editorial.go, internal/schema/editorial_test.go</files>
  <action>
    Create internal/schema/editorial.go with SchemaMeta and LoadEditorialCollectionNames(dir string) per D-54 and 04-PATTERNS.md: scan dir/schema/*.json sorted; unmarshal into SchemaMeta; append meta.Name when Editorial is true and name non-empty; return nil,nil when dir missing (same as sync.go); skip malformed files with continue (no fail-fast). Reuse trimJSON from sync.go — export it or duplicate minimally in same package. Do NOT query app.FindCollectionByNameOrId for editorial flag.
    Write editorial_test.go: temp dir with hero_content.json editorial true and press_releases.json without flag; assert LoadEditorialCollectionNames returns exactly ["hero_content"]; assert missing dir returns nil slice; assert editorial false collection excluded.
  </action>
  <acceptance_criteria>
    - LoadEditorialCollectionNames returns hero_content when only that file has editorial true
    - System collections are never inferred — only explicit editorial true in schema JSON counts
    - Tests pass without PocketBase bootstrap
  </acceptance_criteria>
  <verify>
    <automated>go test ./internal/schema/ -run Editorial -count=1 -timeout 60s</automated>
  </verify>
  <done>PUB-01 editorial discovery implemented from raw schema files per D-54.</done>
</task>

<task type="auto">
  <name>Task 2: hero_content editorial flag in schema fixtures</name>
  <read_first>
    - workspace/schema/hero_content.json — existing SEC-02 rules and fields
    - .planning/phases/04-staging-environments-client-content-publish/04-CONTEXT.md — D-54
    - CLAUDE.md — dual-write workspace + scaffold embed
  </read_first>
  <files>workspace/schema/hero_content.json, internal/scaffold/embed/hero_content.json</files>
  <action>
    Add "editorial": true immediately after "type": "base" in both workspace/schema/hero_content.json and internal/scaffold/embed/hero_content.json per PUB-01 and D-54. Preserve all existing listRule/viewRule/updateRule fields unchanged. Do not add editorial to press_releases or other collections.
  </action>
  <acceptance_criteria>
    - Both hero_content.json files contain "editorial": true
    - grep finds exactly one editorial true per file
  </acceptance_criteria>
  <verify>
    <automated>grep -v '^#' workspace/schema/hero_content.json internal/scaffold/embed/hero_content.json | grep -c '"editorial": true' | grep -qx 2</automated>
  </verify>
  <done>hero_content marked editorial in workspace and init scaffold per PUB-01.</done>
</task>

<task type="auto">
  <name>Task 3: Editorial test fixtures and config template</name>
  <read_first>
    - .planning/phases/04-staging-environments-client-content-publish/04-PATTERNS.md — internal/testutil/content.go and config.example.json
    - internal/testutil/workspace.go — NewWorkspace pattern
    - internal/router/inline_edit_test.go — hero_content schema constant
  </read_first>
  <files>internal/testutil/content.go, workspace/.monms/config.example.json</files>
  <action>
    Add internal/testutil/content.go with NewEditorialWorkspace(t) per 04-PATTERNS: calls NewWorkspace; writes schema/hero_content.json with editorial true and Phase 3 API rules; creates .monms/config.json with productionUrl http://127.0.0.1:0 and publisherEmails ["publisher@test.local"] for tests (gitignored path in temp dir).
    Commit workspace/.monms/config.example.json with productionUrl placeholder https://production.example.com and publisherEmails ["publisher@client.com"] — no secrets. Document in file comment that live config.json and publish-state.json are gitignored (PUB-08 setup).
  </action>
  <acceptance_criteria>
    - NewEditorialWorkspace returns valid workspace passing ValidateWorkspace
    - config.example.json is valid JSON with productionUrl and publisherEmails keys
  </acceptance_criteria>
  <verify>
    <automated>test -f workspace/.monms/config.example.json && go test ./internal/testutil/ -run TestNewEditorialWorkspace -count=1 -timeout 60s 2>/dev/null || go build ./internal/testutil/...</automated>
  </verify>
  <done>Wave 0 fixtures ready for internal/content tests in plan 04-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| schema/*.json → editorial allowlist | Untrusted workspace files define which collections may export/import |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | editorial flag in schema JSON | mitigate | Production import (plan 04-04) rejects collections not in LoadEditorialCollectionNames allowlist |
| T-04-02 | Elevation | marking system collection editorial | mitigate | Parser only reads declared name; import API denylist _superusers and auth collections in plan 04-04 |
</threat_model>

<verification>
- go test ./internal/schema/ -run Editorial -count=1
- Editorial fixtures compile for downstream content package
</verification>

<success_criteria>
- PUB-01 satisfied: editorial true parsed from schema JSON files
- hero_content dual-written with editorial flag
- Test harness NewEditorialWorkspace available
</success_criteria>

<output>
After completion, create `.planning/phases/04-staging-environments-client-content-publish/04-01-SUMMARY.md`
</output>
