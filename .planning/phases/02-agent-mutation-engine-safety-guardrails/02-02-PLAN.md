---
phase: "02"
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - internal/scaffold/init.go
  - internal/scaffold/embed.go
  - internal/scaffold/hook_test.go
autonomous: true
requirements:
  - AGT-05

must_haves:
  truths:
    - "monms init writes workspace/.git/hooks/pre-commit with #!/bin/sh shebang and monms-validate-hook marker"
    - "pre-commit hook file has mode 0755 (executable)"
    - "running monms init again on an already-initialized workspace does NOT overwrite a hook that already contains the monms-validate-hook marker"
    - "running monms init on a workspace where .git/hooks has a non-monms hook file DOES overwrite it (marker absent)"
    - "pre-commit hook discovers monms binary via $MONMS_BIN, then PATH, then ../../monms relative to hook"
    - "pre-commit hook runs git diff --cached | grep .gohtml; exits 0 immediately when no .gohtml staged"
    - "pre-commit hook runs monms validate; if it exits non-zero, runs git checkout -- . then exits 1"
  artifacts:
    - path: "internal/scaffold/init.go"
      provides: "installPreCommitHook(wsRoot string) error + call after maybeGitInit in RunInit"
      contains: "installPreCommitHook"
    - path: "internal/scaffold/hook_test.go"
      provides: "Integration tests for hook install, idempotency, content verification"
      contains: "TestInstallPreCommitHook TestInstallPreCommitHookIdempotent"
  key_links:
    - from: "internal/scaffold/init.go RunInit"
      to: "installPreCommitHook"
      via: "called after maybeGitInit(wsAbs) succeeds"
      pattern: "installPreCommitHook"
    - from: "installPreCommitHook"
      to: "workspace/.git/hooks/pre-commit"
      via: "os.WriteFile(hookPath, preCommitHookScript, 0o755)"
      pattern: "WriteFile.*pre-commit"
---

<objective>
Extend monms init to install an executable pre-commit hook into workspace/.git/hooks/pre-commit. The hook runs monms validate against staged .gohtml files and performs git checkout -- . rollback on failure. Installation is idempotent via a content marker. Cover hook install, idempotency, and hook script content with integration tests.

Purpose: Completes AGT-05 — automatic git rollback on validation failure. Depends on Plan 01 (validate package must exist for the hook to call).

Output: internal/scaffold/init.go extended + hook_test.go tests.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-agent-mutation-engine-safety-guardrails/02-CONTEXT.md
@.planning/phases/02-agent-mutation-engine-safety-guardrails/02-RESEARCH.md
@.planning/phases/02-agent-mutation-engine-safety-guardrails/02-PATTERNS.md

<!-- Reference the Wave 1 summary once created -->

<interfaces>
<!-- Key types and contracts from existing codebase -->

From internal/scaffold/init.go lines 36-70 — RunInit function to extend:
```go
func RunInit(args []string) error {
    // ... MkdirAll, dirs, files, gitkeep files ...
    if err := maybeGitInit(wsAbs); err != nil {
        return err
    }
    // ADD installPreCommitHook call here (before slog.Info)
    slog.Info("workspace initialized", "path", wsAbs)
    return nil
}
```

From internal/scaffold/init.go lines 144-164 — maybeGitInit pattern (IsNotExist + exec.Command with .Dir):
```go
func maybeGitInit(wsRoot string) error {
    gitDir := filepath.Join(wsRoot, ".git")
    if _, err := os.Stat(gitDir); err == nil {
        slog.Info("git repository already exists, skipping git init")
        return nil
    } else if !os.IsNotExist(err) {
        return fmt.Errorf("stat .git: %w", err)
    }
    // ...
}
```

From internal/scaffold/init.go lines 83-108 — writeScaffoldFile idempotency pattern (stat + skip-if-exists):
```go
if _, err := os.Stat(dest); err == nil {
    slog.Info("skip existing scaffold file", "path", destRel)
    return nil
}
```
Note: installPreCommitHook uses CONTENT check (bytes.Contains for marker) not just file existence — per D-40.

From internal/scaffold/embed.go:
```go
//go:embed embed/*
var scaffoldFS embed.FS
```
Pattern for embedding the hook script: add as package-level const string in init.go (no separate embed file needed for a small script).

From internal/scaffold/init_test.go — existing test package for reference:
```go
package scaffold_test
// Uses t.TempDir(), RunInit([]string{"--workspace=" + ws})
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add installPreCommitHook to internal/scaffold/init.go and call from RunInit</name>
  <files>internal/scaffold/init.go</files>
  <read_first>
    - internal/scaffold/init.go full file: understand maybeGitInit pattern, RunInit flow, existing imports (bytes not yet imported)
    - .planning/phases/02-agent-mutation-engine-safety-guardrails/02-RESEARCH.md Pattern 4: full pre-commit hook bash script content and installPreCommitHook implementation
    - .planning/phases/02-agent-mutation-engine-safety-guardrails/02-PATTERNS.md installPreCommitHook section: idempotency guard with bytes.Contains, os.MkdirAll(hooksDir) for A4
    - .planning/phases/02-agent-mutation-engine-safety-guardrails/02-RESEARCH.md Pitfall 3: hook must go in workspace/.git/hooks/, not engine repo
    - .planning/phases/02-agent-mutation-engine-safety-guardrails/02-RESEARCH.md Pitfall 5: marker check must use bytes.Contains on content, not bare file-existence check
  </read_first>
  <action>
Add "bytes" to the import block of internal/scaffold/init.go (it is not yet imported).

Add a package-level constant preCommitHookScript in init.go (or a new file internal/scaffold/hook.go — either is fine, prefer keeping in init.go to minimize file count). The constant contains the full bash pre-commit script per D-40, D-41, D-42, D-43:

```
#!/bin/sh
# monms-validate-hook — DO NOT REMOVE THIS COMMENT (idempotency marker)

if [ -n "$MONMS_BIN" ]; then
  MONMS="$MONMS_BIN"
elif command -v monms >/dev/null 2>&1; then
  MONMS="monms"
else
  HOOK_DIR="$(cd "$(dirname "$0")" && pwd)"
  CANDIDATE="$HOOK_DIR/../../monms"
  if [ -x "$CANDIDATE" ]; then
    MONMS="$(cd "$(dirname "$CANDIDATE")" && pwd)/$(basename "$CANDIDATE")"
  else
    echo "monms: binary not found" >&2
    echo "  Set MONMS_BIN, add monms to PATH, or place binary at ../../monms relative to workspace" >&2
    exit 1
  fi
fi

STAGED=$(git diff --cached --name-only --diff-filter=ACM | grep '\.gohtml$')
if [ -z "$STAGED" ]; then
  exit 0
fi

if ! echo "$STAGED" | tr '\n' '\0' | xargs -0 "$MONMS" validate; then
  echo "" >&2
  echo "Pre-commit validation failed. Rolling back workspace to last stable state..." >&2
  git checkout -- .
  echo "Workspace restored. Fix the errors above, then re-apply your changes." >&2
  exit 1
fi

exit 0
```

Add installPreCommitHook(wsRoot string) error function:
- hooksDir = filepath.Join(wsRoot, ".git", "hooks")
- If os.Stat(hooksDir) is NotExist: os.MkdirAll(hooksDir, 0o755) to handle A4 (hooks dir may be absent right after git init on some systems); if Stat returns any non-NotExist error, return fmt.Errorf("stat hooks dir: %w", err)
- hookPath = filepath.Join(hooksDir, "pre-commit")
- If os.ReadFile(hookPath) succeeds AND bytes.Contains(existing, []byte("monms-validate-hook")): slog.Info("pre-commit hook already installed, skipping"); return nil
- os.WriteFile(hookPath, []byte(preCommitHookScript), 0o755)
- On error: return fmt.Errorf("install pre-commit hook: %w", err)
- slog.Info("pre-commit hook installed", "path", hookPath)
- return nil

Extend RunInit: after the `if err := maybeGitInit(wsAbs); err != nil { return err }` block, add:
```go
if err := installPreCommitHook(wsAbs); err != nil {
    return err
}
```
This must appear before the `slog.Info("workspace initialized", ...)` line.

Boundary guard: installPreCommitHook receives wsAbs (workspace root) — hooksDir is filepath.Join(wsAbs, ".git", "hooks"). This writes to workspace's git repo, NOT the engine source repo (per RESEARCH Pitfall 3). The wsAbs value comes from config.ResolveWorkspace in RunInit.
  </action>
  <verify>
    <automated>cd /home/terence/code/MonMS && go build ./internal/scaffold/... 2>&amp;1 &amp;&amp; echo "compile ok"</automated>
  </verify>
  <done>
    - internal/scaffold/init.go compiles without errors
    - installPreCommitHook function present in scaffold package
    - preCommitHookScript constant contains "monms-validate-hook" marker
    - RunInit calls installPreCommitHook(wsAbs) after maybeGitInit
    - "bytes" in import block
  </done>
</task>

<task type="auto">
  <name>Task 2: Create internal/scaffold/hook_test.go with hook install and idempotency tests</name>
  <files>internal/scaffold/hook_test.go</files>
  <read_first>
    - internal/scaffold/init_test.go: existing test file in package scaffold_test — mirror package declaration, import style, t.TempDir() usage, RunInit invocation pattern
    - .planning/phases/02-agent-mutation-engine-safety-guardrails/02-PATTERNS.md hook_test.go section: full test implementations for TestInstallPreCommitHook, TestInstallPreCommitHookIdempotent
    - .planning/phases/02-agent-mutation-engine-safety-guardrails/02-RESEARCH.md Validation Architecture: TestPreCommitHookRollback listed as AGT-05 test
    - .planning/phases/02-agent-mutation-engine-safety-guardrails/02-RESEARCH.md Pitfall 4: git checkout -- . does not remove new untracked files — document this limitation in test comment, skip full exec test if git unavailable
  </read_first>
  <action>
Create internal/scaffold/hook_test.go with package scaffold_test. Imports: "bytes", "os", "path/filepath", "strings", "testing", "github.com/monms/monms/internal/scaffold".

Implement these tests:

TestInitInstallsPreCommitHook:
  - ws = t.TempDir()
  - Call scaffold.RunInit([]string{"--workspace=" + ws})
  - hookPath = filepath.Join(ws, ".git", "hooks", "pre-commit")
  - If git is not in PATH, t.Skip("git not in PATH, skipping hook install test") (use exec.LookPath in the test)
  - data, err = os.ReadFile(hookPath); if err != nil t.Fatalf("hook not created: %v", err)
  - Assert strings.HasPrefix(string(data), "#!/bin/sh") — shebang present
  - Assert strings.Contains(string(data), "monms-validate-hook") — marker present
  - Assert strings.Contains(string(data), "git checkout -- .") — rollback present (AGT-05)
  - Check file mode: fi, _ := os.Stat(hookPath); assert fi.Mode().Perm()&0111 != 0 (executable bit set)

TestInitPreCommitHookIdempotent:
  - ws = t.TempDir()
  - Pre-create .git/hooks/ manually: os.MkdirAll(filepath.Join(ws, ".git", "hooks"), 0o755)
  - Write a hook file with the marker: os.WriteFile(hookPath, []byte("# monms-validate-hook\noriginal content"), 0o755)
  - Call scaffold.RunInit([]string{"--workspace=" + ws}) — must skip hook install
  - Read hookPath again; assert content is still "# monms-validate-hook\noriginal content" unchanged

TestInitPreCommitHookOverwritesNonMonmsHook:
  - ws = t.TempDir()
  - Pre-create .git/hooks/ manually
  - Write a hook without the marker: os.WriteFile(hookPath, []byte("#!/bin/sh\n# other tool\nexit 0"), 0o755)
  - Call scaffold.RunInit([]string{"--workspace=" + ws})
  - Read hookPath; assert the content now contains "monms-validate-hook" (overwritten per D-40)

TestPreCommitHookRollback (smoke — validates hook script content, not exec):
  - This test validates that the hook script CONTAINS git checkout -- . in the failure path
  - ws = t.TempDir()
  - os.MkdirAll(filepath.Join(ws, ".git", "hooks"), 0o755)
  - scaffold.RunInit([]string{"--workspace=" + ws})
  - data, _ = os.ReadFile(filepath.Join(ws, ".git", "hooks", "pre-commit"))
  - Assert bytes.Contains(data, []byte("git checkout -- ."))
  - Assert bytes.Contains(data, []byte("exit 1"))
  - Comment: "Full exec rollback test is in integration (Wave 3 press_releases test covers AGT-03/AGT-04). Hook script correctness verified by content inspection."

Note on import for exec.LookPath: add "os/exec" to imports for the git-availability guard in TestInitInstallsPreCommitHook.
  </action>
  <verify>
    <automated>cd /home/terence/code/MonMS && go test ./internal/scaffold/... -count=1 -v -run "TestInit|TestPreCommit" 2>&amp;1 | tail -30</automated>
  </verify>
  <done>
    - All hook tests pass: TestInitInstallsPreCommitHook (or skipped if git absent), TestInitPreCommitHookIdempotent, TestInitPreCommitHookOverwritesNonMonmsHook, TestPreCommitHookRollback
    - `go test ./internal/scaffold/... -count=1` exits 0
    - Existing scaffold init tests (from init_test.go) still pass
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wsRoot arg → hook write path | Hook path is derived from wsRoot; must stay inside workspace/.git/hooks/ |
| agent commit → pre-commit hook execution | Hook executes monms validate as subprocess; monms binary path from env/PATH |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | pre-commit hook bypass via git commit --no-verify | accept | Document limitation in SECURITY.md; hook is advisory for agent; server-side enforcement is Phase 3+ responsibility |
| T-02-06 | Elevation of privilege | installPreCommitHook writes outside workspace via malformed wsRoot | mitigate | hookPath derived as filepath.Join(wsRoot, ".git", "hooks", "pre-commit"); wsRoot already validated by config.ResolveWorkspace before RunInit calls installPreCommitHook |
| T-02-07 | Tampering | existing non-monms hook overwritten silently | accept | D-40 specifies overwrite when marker absent; slog.Info("pre-commit hook installed") logged; operator must check before monms init if they have an existing hook they wish to keep |
</threat_model>

<verification>
Full scaffold package after both tasks:
```
cd /home/terence/code/MonMS
go test ./internal/scaffold/... -count=1 -race -v 2>&1 | tail -30
```
Manual smoke: `monms init --workspace /tmp/test-ws2 && cat /tmp/test-ws2/.git/hooks/pre-commit | head -3`
Expected output: `#!/bin/sh` + `# monms-validate-hook`
</verification>

<success_criteria>
- installPreCommitHook added to internal/scaffold, called from RunInit after maybeGitInit
- Hook script contains: shebang #!/bin/sh, monms-validate-hook marker, MONMS_BIN/$PATH/../../monms binary discovery, git diff --cached .gohtml filter, git checkout -- . rollback, exit 1 on failure
- Hook file mode is 0755 (executable)
- Idempotency: second RunInit on hook-present workspace does NOT overwrite when marker present
- Overwrite: RunInit DOES overwrite hook without monms-validate-hook marker
- All scaffold tests pass including existing ones
- `go test ./... -count=1 -short` still green (Phase 1 + Phase 2 so far)
</success_criteria>

<output>
After completion, create .planning/phases/02-agent-mutation-engine-safety-guardrails/02-02-SUMMARY.md
</output>
